name: Harness E2E campaign runner

on:
  workflow_call:
    inputs:
      campaign:
        description: Checked-in campaign id under config/campaigns
        required: true
        type: string
      timeout_minutes:
        description: Maximum wall time for the complete campaign
        required: true
        type: number

permissions:
  contents: read

jobs:
  campaign:
    if: endsWith(github.workflow_ref, '@refs/heads/main')
    runs-on: [self-hosted, harness-e2e]
    environment: harness-e2e-trusted
    timeout-minutes: ${{ inputs.timeout_minutes }}
    env:
      HARNESS_E2E_MODEL: ${{ vars.HARNESS_E2E_MODEL }}
      HARNESS_E2E_PROVIDER: ${{ vars.HARNESS_E2E_PROVIDER }}
      HARNESS_E2E_ENGINEERING_FIXTURE_REPOSITORY: ${{ vars.HARNESS_E2E_ENGINEERING_FIXTURE_REPOSITORY }}
      ENGINEERING_TICKET_FIXTURE_REVISION: 7a6b25b3cd12d66af74a358ae86e0d2b846bd384
      SHARED_FIXTURE_REVISION: 16f6b9e05e34e09c824191eed0631d77f85be6a9
      III_URL: ${{ vars.III_URL }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: pnpm/action-setup@v4
        with:
          version: 11.13.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: dashboard/pnpm-lock.yaml
      - run: pnpm --dir dashboard install --frozen-lockfile
      - run: pnpm --dir dashboard build
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build the trusted evaluator
        run: cargo build --locked --release --bin harness-e2e
      - name: Validate the campaign and trusted runtime
        env:
          CAMPAIGN: ${{ inputs.campaign }}
        run: |
          python3 scripts/run_e2e_campaign.py "config/campaigns/${CAMPAIGN}.json" --validate-only
          test -x /opt/iii-harness-e2e/archive-trusted-execution
          test -n "${HARNESS_E2E_MODEL:-}"
          test -n "${HARNESS_E2E_PROVIDER:-}"
          test -n "${III_URL:-}"
          test -x /opt/iii-harness-e2e/engineering-ticket-fixture
          test -n "${HARNESS_E2E_ENGINEERING_FIXTURE_REPOSITORY:-}"
          if [[ "$CAMPAIGN" == "weekly" ]]; then
            test -x /opt/iii-harness-e2e/run-weekly-stress
          fi
      - name: Prepare the disposable code fixtures
        run: |
          execution_id="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          engineering_json="$RUNNER_TEMP/engineering-ticket-fixture-${execution_id}.json"
          shared_json="$RUNNER_TEMP/shared-fixture-${execution_id}.json"
          /opt/iii-harness-e2e/engineering-ticket-fixture prepare \
            --execution-id "${execution_id}-engineering" \
            --revision "$ENGINEERING_TICKET_FIXTURE_REVISION" > "$engineering_json"
          engineering_lease=$(jq -er .lease_id "$engineering_json")
          trap '/opt/iii-harness-e2e/engineering-ticket-fixture cleanup --lease-id "$engineering_lease"' ERR
          jq -e \
            --arg revision "$ENGINEERING_TICKET_FIXTURE_REVISION" \
            '.revision == $revision and (.path | startswith("/")) and (.lease_id | length == 32)' \
            "$engineering_json"
          /opt/iii-harness-e2e/engineering-ticket-fixture prepare \
            --execution-id "${execution_id}-shared" \
            --revision "$SHARED_FIXTURE_REVISION" > "$shared_json"
          shared_lease=$(jq -er .lease_id "$shared_json")
          trap 'trap - ERR; /opt/iii-harness-e2e/engineering-ticket-fixture cleanup --lease-id "$shared_lease" || true; /opt/iii-harness-e2e/engineering-ticket-fixture cleanup --lease-id "$engineering_lease" || true' ERR
          jq -e \
            --arg revision "$SHARED_FIXTURE_REVISION" \
            '.revision == $revision and (.path | startswith("/")) and (.lease_id | length == 32)' \
            "$shared_json"
          echo "HARNESS_E2E_ENGINEERING_TICKET_FIXTURE_PATH=$(jq -er .path "$engineering_json")" >> "$GITHUB_ENV"
          echo "HARNESS_E2E_ENGINEERING_TICKET_FIXTURE_LEASE=$engineering_lease" >> "$GITHUB_ENV"
          echo "HARNESS_E2E_FIXTURE_PATH=$(jq -er .path "$shared_json")" >> "$GITHUB_ENV"
          echo "HARNESS_E2E_SHARED_FIXTURE_LEASE=$shared_lease" >> "$GITHUB_ENV"
          trap - ERR
      - name: Run the advisory campaign
        env:
          CAMPAIGN: ${{ inputs.campaign }}
        run: |
          execution_id="${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          python3 scripts/run_e2e_campaign.py "config/campaigns/${CAMPAIGN}.json" \
            --advisory \
            --e2e-bin "$PWD/target/release/harness-e2e" \
            --output-root "$PWD/campaign-results" \
            --execution-id "$execution_id" \
            --summary "$PWD/campaign-results/campaign-summary.json" \
            --bundle "$PWD/campaign-results/campaign-bundle.json" \
            --fault-runner /opt/iii-harness-e2e/run-weekly-stress
      - name: Remove the disposable code fixtures
        if: always()
        run: |
          shared_lease="${HARNESS_E2E_SHARED_FIXTURE_LEASE:-}"
          engineering_lease="${HARNESS_E2E_ENGINEERING_TICKET_FIXTURE_LEASE:-}"
          cleanup_failed=0
          if [[ -n "$shared_lease" ]]; then
            /opt/iii-harness-e2e/engineering-ticket-fixture cleanup \
              --lease-id "$shared_lease" || cleanup_failed=1
          fi
          if [[ -n "$engineering_lease" ]]; then
            /opt/iii-harness-e2e/engineering-ticket-fixture cleanup \
              --lease-id "$engineering_lease" || cleanup_failed=1
          fi
          exit "$cleanup_failed"
      - name: Archive every materialized group
        if: always()
        run: |
          summary="$PWD/campaign-results/campaign-summary.json"
          test -f "$summary" || exit 0
          while IFS= read -r output; do
            results="$output/results.json"
            manifest="$output/manifest.json"
            if [[ ! -f "$results" || ! -f "$manifest" ]]; then
              continue
            fi
            execution_id=$(jq -er .execution.execution_id "$manifest")
            archive=(
              /opt/iii-harness-e2e/archive-trusted-execution
              --execution-id "$execution_id"
              --retention longitudinal
              --include "$manifest"
              --include "$results"
              --include "$summary"
              --output "$output/archive-head.json"
            )
            if [[ -f "$output/observation.json" ]]; then
              archive+=(--include "$output/observation.json")
            fi
            "${archive[@]}"
            jq -e '
              .availability == "available" and
              (.archive.manifest.uri | startswith("iii-storage://")) and
              ((.archive.manifest.sha256 | sub("^sha256:"; "") | length) == 64)
            ' "$output/archive-head.json"
          done < <(jq -r '.groups[].output' "$summary")
          while IFS= read -r evaluation; do
            output=$(dirname "$evaluation")
            execution_id=$(jq -er .execution_id "$evaluation")
            archive=(
              /opt/iii-harness-e2e/archive-trusted-execution
              --execution-id "$execution_id"
              --retention longitudinal
              --include "$(dirname "$output")/fault-plan.json"
              --include "$output/fault-journal.json"
              --include "$evaluation"
              --include "$summary"
              --output "$output/archive-head.json"
            )
            if [[ -f "$output/results/results.json" ]]; then
              archive+=(--include "$output/results/results.json")
            fi
            "${archive[@]}"
          done < <(find "$PWD/campaign-results" -path '*/run-*/fault-evaluation.json' -type f | sort)
      - name: Publish the advisory summary
        if: always()
        run: |
          summary="$PWD/campaign-results/campaign-summary.json"
          if [[ -f "$summary" ]]; then
            jq -r '
              "### Harness E2E campaign: \(.campaign_id)\n",
              "Objective passed: `\(.objective_passed)`\n",
              (.groups[] | "- `\(.group_id)`: \(.status) (exit=\(.exit_code // "n/a"), duration=\(.duration_ms)ms)")
            ' "$summary" >> "$GITHUB_STEP_SUMMARY"
          else
            echo 'Harness E2E campaign did not produce a summary.' >> "$GITHUB_STEP_SUMMARY"
          fi
      - name: Retain a short diagnostic fallback
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: harness-e2e-${{ inputs.campaign }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: campaign-results
          retention-days: 30
          if-no-files-found: warn
