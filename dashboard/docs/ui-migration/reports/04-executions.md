## Executions (rota `#/ext/harness-e2e/executions`)

### Anatomia atual
- Chrome do console: barra de abas, cabeçalho "harness e2e · Executions · evidence, plans and live evaluation control", ações "new plan / quick run" (injetadas por `components/AppHeader.tsx:48-56` via `DashboardShell`, só em modo local) e as sub-abas Overview / Tests / Executions / Plans (renderizadas pelo console como `button.iii-ui-tab`, fora de `.ds-root`).
- `pages/OverviewPage.tsx:776` — com `activeView='executions'` a página renderiza apenas `<ExecutionHistory>` (`OverviewPage.tsx:372-548`), o mesmíssimo componente que a Overview usa em `:771`, sem o hero "Latest execution" e sem o aside.
- Rótulo H2 "RECENT EXECUTIONS" em 11px uppercase `#a3a09c` (`:402-407`) + contador "20 of 20 executions" 12px (`:408-410`), 45px abaixo do nav (`pt-5` em `:699` + `mt-6` em `:400`).
- Filtros (`:412-453`): input de busca (sans do sistema, 13px), select "All statuses" e select "All triggers" (Chivo Mono 13px) em grid `974px 220px 150px`.
- Tabela (`:454-539`): `font-mono text-xs md:text-[13px]`, `min-w-[56rem]`, dentro de `div.overflow-x-auto`; colunas Execution (link do label + data 10.4px), Result (`StatusBadge` derivado em `statusCopy` `:39-96`), Subject (`provider/model` cru), Scope ("recebidos/esperados" + "% coverage"), Outcome (pass rate + "N judge model events" ou "No blocking events"), Efficiency (wall time + tokens), Evidence (pill `rounded-full` "Diagnostic detail" / "Aggregate" / "No report", `:526-534`).
- Dados: `listExecutions({ limit: 100 })` (`:589`); busca e filtros 100% client-side (`:380-398`); `next_cursor` e os parâmetros server-side `query/status/event` existem (`lib/dashboard-data-source.ts:387`, `:1040-1046`) mas não são usados; nenhuma assinatura de `subscribeRunChanges` (só `pages/TestsPage.tsx:443`).
- Estados: loading = skeleton (`:713-722`); erro = painel com Retry (`:701-712`); 0 execuções = card "No executions yet" (`:725-766`); 0 resultados de filtro = "No executions match these filters." (`:540-544`).

### Achados
| # | Sev | Categoria | Achado (o que está errado, com evidência: screenshot + arquivo:linha) | Recomendação concreta |
|---|-----|-----------|-----------|----------------|
| E-01 | P0 | responsivo | Abaixo de ~950px a seção estoura e o conteúdo fica inacessível: em 720 o `main` mede 662px mas o grid de filtros e a tabela medem 1360px (sonda DOM: `section` w=1360 com `min-width:auto`; wrapper `overflow-x-auto` w=1360); como `.harness-e2e-dashboard` é `overflow-x-hidden` (`components/DashboardShell.tsx:249`) não há scroll horizontal possível. O select "All triggers" fica em x=713 num painel de 686px e as colunas Scope/Outcome/Efficiency/Evidence somem (executions-light-720.png, crop-exec-720-filters.png); em 390 sobra só Execution e metade de Result (executions-light-390.png). Causa: `#overview-content` é `grid` (`OverviewPage.tsx:724`) e o `<section className="mt-6">` (`:400`) não tem `min-w-0`; na Overview o mesmo componente vai dentro de `<div className="min-w-0">` (`:769`) e por isso não quebra lá. | Adicionar `min-w-0` à `<section>` de `ExecutionHistory` (ou envolver como na Overview). Fix testado via sonda: com `#overview-content > * { min-width:0 }` o wrapper passa a 662/366px com scroll interno (scrollWidth 1360) e o grid de filtros volta a 2 colunas (select trigger em x=364). Adicionar sombra/indicador de scroll no wrapper. |
| E-02 | P0 | IA / feedback | O vocabulário do filtro não é o da coluna Result. A coluna deriva o rótulo de `assessment_summary` (`statusCopy` `:39-96`: "Failed", "Hard gate failed"), o select filtra por `execution.status` cru (`:383`, opções `:431-438`: `technical_failed`, `infra_failed`…), que o servidor calcula com precedência technical > hard_gate > passed > infra (`src/dashboard/presenter.rs:294-304`). Teste real: a tabela mostra 5x "hard gate failed", mas o filtro "Hard gate failed" devolve 3; "Technical failure" devolve 4 (2 "failed" + 2 "hard gate failed"); "Infrastructure failure", "Running" e "Incomplete" devolvem 0 sem indicar que a opção não existe no conjunto. | Filtrar pelo mesmo estado derivado que a coluna mostra (ou mostrar o status cru na coluna); mostrar contagem por opção ("hard gate failed (5)") e desabilitar opções com 0; usar os parâmetros server-side `status/event/query` já existentes em `dashboard-data-source.ts:1043-1045`. |
| E-03 | P1 | copy / estados | O placeholder promete "Search label, run, or date" (`:420`) mas a busca compara com `completed_at` em ISO (`:386-392`): "Aug 26" (formato exibido) devolve 0, "2026-08-26" devolve 3; o subject não entra no índice ("terra" devolve 0). | Indexar `formatDate(completed_at)`, `provider/model` e o `sha` curto; placeholder "search label, model, id or date". |
| E-04 | P1 | IA / URL | Filtros e busca não vão para a URL nem sobrevivem à navegação: com "Passed" selecionado ("10 of 20"), abrir uma execução e voltar reinicia para "All" ("20 of 20") — estado só em `useState` (`:377-379`). | Serializar `status/event/q` no hash (`executions?status=passed&q=…`) e restaurar no mount; fallback em `sessionStorage`. |
| E-05 | P1 | estados / escala | `limit: 100` (`:589`) e contador `filtered.length of executions.length` (`:408-410`): com mais de 100 execuções a tela mostrará "100 of 100 executions" e esconde o resto silenciosamente. Não há paginação, "load more" nem ordenação (nenhum `th` clicável, `:456-466`), embora o manifesto entregue `next_cursor` (`dashboard-data-source.ts:387`, `:1020-1027`). | "Load more" por cursor + ordenação por Completed / Result / Runtime / Tokens; contador "showing 20 of 137"; ordem padrão explícita ("newest first"). |
| E-06 | P1 | IA / redundância | A aba Executions é literalmente a `ExecutionHistory` da Overview (mesmo componente, mesmos 100 itens, mesmos filtros; `OverviewPage.tsx:771` vs `:776`). A única diferença é a ausência do hero. Duas abas, uma lista. | Overview = latest + 5 últimas + link "view all executions →"; Executions = tabela completa com paginação, ordenação, filtros server-side e colunas configuráveis. |
| E-07 | P1 | hierarquia | A página não tem título nem H1: o único heading é o H2 "RECENT EXECUTIONS" em 11px `#a3a09c` (sonda: headings = 1), flutuando 45px abaixo do nav (executions-light-1440.png). "Recent" é falso para a lista completa. | `PageHeader` "executions" (h1 1rem mono) com resumo "20 executions · 10 passed · 7 failed · 3 cancelled" no lugar do rótulo; remover o `mt-6`. |
| E-08 | P1 | feedback / alvo | A linha inteira ganha hover (`[&_tbody_tr:hover]:bg-[var(--surface-soft)]`, `:455`) e tem 53px de altura, mas só o texto do label (192x15px, sem sublinhado, `text-ink` weight 500, `:474-476`) é clicável; `cursor: auto` na linha (sonda). Clicar em qualquer outra célula não faz nada. | Linha inteira clicável (link com `::after` cobrindo a `tr`, ou `onClick` + `role="link"` na `tr`) e `cursor-pointer`; ou remover o hover da linha e sublinhar o link. |
| E-09 | P1 | copy / densidade | Coluna Evidence repete o pill "Diagnostic detail" em 17 de 20 linhas (`:526-534`): é um `<span>` não interativo com cara de botão (`rounded-full` + preenchimento), o rótulo soa como ação, e no detalhe o mesmo atributo (`availability='full'`) chama-se "Full evidence" com raio 6px (`pages/ExecutionPage.tsx:733-739`). | Remover a coluna; sinalizar só a exceção ("no report" / "aggregate") como texto muted junto ao Result, ou como ícone com `title`. |
| E-10 | P1 | copy / estados | Linhas canceladas encadeiam placeholders: Subject "Not reported", Scope "—/— · Not reported coverage" (concatenação `${formatPercent()} coverage`, `:497` + `lib/execution-view.ts:282`), Outcome "Not reported · No blocking events" (`:510` — afirmação positiva para uma execução cancelada), Efficiency "Not reported · Tokens not reported": 5 "Not reported" por linha (executions-light-1440.png, linhas "context impact", "Aug 22 5:19 PM", "Aug 21 9:41 PM"). | Para `availability='unavailable'`, colapsar Scope/Outcome/Efficiency/Evidence numa célula "cancelled after 12m · no report"; nunca mostrar "No blocking events" fora de `passed`. |
| E-11 | P1 | A11Y contraste | O fundo real do painel é `#f2f0ed` (`--color-bg`, sonda `rootBg`), não `#fafafa`. Datas e sub-linhas em 10.4px `#a3a09c` (20 nós) = 2.29:1; cabeçalhos de coluna 11px mesmo cinza; badges "failed" / "hard gate failed" 12px `#ff0026` = 3.50:1 (7 linhas); "cancelled" 12px ghost 2.29:1. Nada atinge AA 4.5:1 para texto pequeno. No escuro ghost `#6f6f6f` sobre `#111` = 3.76:1 (ainda reprova). | Sub-linhas em `--color-ink-faint` (`#6b6865` = 4.87:1) e piso 11px; para texto de alerta pequeno usar `#d81b3c` (5.0:1) e reservar `#ff0026` para o ponto do status. |
| E-12 | P1 | estados / tempo real | A lista carrega uma vez (`:599-601`) e não assina `subscribeRunChanges` (só `TestsPage.tsx:443`): uma execução "running" não muda de status sem F5 e não há botão de refresh na tela. `cancelling` nunca chega ao cliente (`presenter.rs:50` mapeia Cancelling para "running"), então o ramo "Cancelling" de `statusCopy` (`:52-57`) é código morto. | Assinar mudanças e refazer `load()` com debounce; badge "running · 4m 12s" com relógio; propagar `cancelling` do servidor. |
| E-13 | P2 | consistência | No mesmo grid de filtros o input usa a pilha sans do sistema (`-apple-system…`, sonda) e os selects usam Chivo Mono; os selects têm cor `--color-ink-faint` (`:413`), a mesma do placeholder, então "All statuses" parece vazio mesmo com valor. | `font-mono lowercase text-[13px]` nos três controles; `text-ink` quando há valor, faint só no placeholder. |
| E-14 | P2 | consistência vocab | Pill com `rounded-full` (border-radius 3.4e7px medido) contra o raio único de 6px; Subject como caminho cru `openai-codex/codex/gpt-5.6-terra` (`:488`) enquanto o detalhe mostra "GPT-5.6 Terra / openai-codex" (`ExecutionPage.tsx:131-140`): mesmo modelo, dois nomes. | Um `formatModel()` compartilhado (nome amigável + provider em `title`); `rounded-[6px]`. |
| E-15 | P2 | A11Y | Skip-link "Skip to execution dashboard" com `href="#main"` (`:669-671`): no console com hash-router, Enter troca a URL para `#main` (sonda: `location.hash === '#main'`, foco fica no `BODY`); não pula para o conteúdo e corrompe a rota (um reload cai fora do dashboard). | `onClick` com `preventDefault()` + `document.getElementById('main').focus()` (`tabIndex={-1}` no `main`). |
| E-16 | P2 | densidade | Scope "2/2 · 100% coverage" é 100% em 17 de 20 linhas (sem variância); Outcome mistura pass rate com "1 judge model event" (`execution-view.ts:317-323` — "event" é vago). | Fundir Scope + Outcome em "1/2 passed · 1 judge error"; mostrar coverage só quando < 100%. |
| E-17 | P2 | navegação (cross-ref S) | Em 720/390 as sub-abas Overview/Tests/Executions/Plans (do console) somem (rect 0x0 na sonda) e ficam atrás de um "⋮" sem rótulo ao lado de "harness e2e" (executions-light-390.png); a lista não tem nenhum outro link para Overview/Plans. | Garantir um `aria-label="sections"` no menu e tabs roláveis em vez de ocultas; no dashboard, breadcrumb "overview / executions" também na lista. |

### Oportunidades de redesenho
- Fundir Overview e Executions: Overview vira "latest + 5 recentes + link"; Executions vira a tabela de verdade (paginação por cursor, ordenação por coluna, filtros server-side sincronizados na URL, colunas configuráveis).
- Novo componente "execution row": ponto de status + label + subject na primeira célula, "scenarios passed/total" com mini barra, "runtime · tokens", data; linha inteira clicável; sem colunas Evidence/Scope.
- Agrupar por plano/data: hoje runs de planos ("Test plan · candidate", "Escadas — degrau 3 · baseline") se misturam com runs ad hoc ("e2e::* control-plane run") na mesma lista plana; um agrupamento por dia ou por plano, com link para `plans/<id>`, dá contexto.
- Faixa "running now" no topo (mesmo componente do detalhe): progresso "3/6 scenarios", tempo decorrido, Cancel; some quando não há execução ativa.

### Quick wins (só CSS/copy, < 1h cada)
- `min-w-0` na `<section>` de `ExecutionHistory` (`OverviewPage.tsx:400`) — conserta 720/390.
- Filtro de status com os rótulos derivados da coluna (ou remover "Technical failure" / "Infrastructure failure" / "Incomplete" / "Running" enquanto não têm ocorrência).
- Indexar a data formatada e o modelo na busca; placeholder "search label, model, id or date".
- `cursor-pointer` + link cobrindo a linha; ou remover o hover da `tr`.
- Sub-linhas para 11px `--color-ink-faint`; badges de alerta pequenos em `#d81b3c`.
- `rounded-full` → `rounded-[6px]`; "Diagnostic detail" → "full"; "Not reported coverage" → "coverage n/a"; "No blocking events" → "—" fora de `passed`.
- Skip-link com `preventDefault` + `focus()`.
- Selects em `font-mono lowercase text-ink`.

## Execution detail (rota `#/ext/harness-e2e/execution/<id>[/summary|results|technical]`)

### Anatomia atual
- `pages/ExecutionPage.tsx:703` — `<DashboardPageActions active="executions"/>` sem `actions`: a aba Executions fica ativa no console, mas "new plan / quick run" desaparecem do cabeçalho.
- `main.page-shell.detail-shell` de 1358px (`:704-707`); breadcrumbs "Overview / Executions / <label>" em 10.24px mono (`:708-717`, CSS legado `index.css:2302-2316`).
- `PageHeader` (`:718-742`): eyebrow "Execution detail", H1 20px mono lowercase = `label` da execução, subtítulo boilerplate "N of N expected reports received. Objective outcomes remain authoritative; advisory AI is shown separately.", ações = `StatusBadge` + badge "AI: …" (só com 1 cenário) + pill "Full evidence / Aggregate / No report".
- `Panel` de identidade: 4 células Scenarios / Subject / Judge / Completed (`:748-772`) + 8 `MetricCard` (`:773-885`): Objective scenarios, Report coverage, Execution runtime, Workflow scenarios, Total tokens, Turns, Function calls, Reported cost.
- `#summary` `DecisionSection` (`:281-416`): eyebrow "Decision", H2 "<system> objectively; AI <verdict>", badges "system: … / ai: …", caixa bordada com "What happened" / "Recommended next step" / "Primary concern" / botão quiet "inspect retained evidence" + coluna com 3 `DecisionBoundary` (Objective system / Advisory AI / Effective harness); abaixo 4 tiles Assessments / Passed / Partial / Evidence references (`:397-413`).
- `#results` `ResultsSection` (`:418-462`): H2 "Scenario results" + `<ScenarioMatrix>` (linhas expansíveis → `AssessmentWorkspace` com "Open chat", filtros "All · N …", rodapé "Transcript / Open chat / Review evidence").
- `#technical` `TechnicalSection` (`:464-526`): H2 "Raw fields and immutable identity", 9 cards bordados com id, run id, attempt, status, source, release, event, actor, started/completed, availability, e `<details>` "Preview raw JSON".
- Footer "Harness E2E · public execution report" + "Back to all executions" (`:908-916`).
- Dados/estados: dois fetches sequenciais uma única vez (`:559-583`), sem polling; loading = texto "Loading execution report…" (`:657-674`); erro = "Execution not found" (`:625-656`). Alturas medidas em 1440x900 (execução com falha): header 118 + identidade 79 + métricas 230 + decisão 575 + resultados 778 + técnico 680 + footer 93 ≈ 2.8k px (a captura de 5000px tem ~2.2k px em branco no fim); hard gate 3.0k; em 390px 5.3k.

### Achados
| # | Sev | Categoria | Achado (o que está errado, com evidência: screenshot + arquivo:linha) | Recomendação concreta |
|---|-----|-----------|-----------|----------------|
| ED-01 | P0 | hierarquia / navegação | Ao abrir uma execução com `attention === 'needs_attention'` a página faz `scrollIntoView('#results')` (`:612-623`): em 1440x900 o container já abre com scrollTop 1071 de 2871 (390: 2256 de 5318). Breadcrumb (y=-892), H1 (y=-803), identidade, métricas e toda a seção Decision (y=-382) ficam acima da dobra (probe-exec-failed-light-1440.png abre em "scenario results"; exec-failed-light-1440.png abre em "separately."). O usuário não vê qual execução abriu nem o status. Passed/cancelled abrem no topo (scrollTop 0): comportamento inconsistente. | Remover o auto-scroll. Se a intenção é destacar falhas, o Decision já está no topo; oferecer um jump-link "go to failing scenarios" e a barra de seções do ED-07. |
| ED-02 | P0 | engano | Execução cancelada sem relatório: tile "Objective scenarios" mostra valor "—" com delta verde "passed" (`:786` `scenarioAttention > 0 ? 'Needs attention' : 'Passed'`, tone `positive` em `:787`) (exec-cancelled-light-1440.png; sonda `label: "Objective scenariosPassed", value: "—"`). Ao lado, "Report coverage · not reported" e "Execution runtime · observed · 0 observed runs". | Tone `unavailable` e delta "not run" quando `scenarioCount === 0` ou status em {cancelled, running, incomplete}; ou esconder os tiles nesses estados (ver ED-11). |
| ED-03 | P0 | engano | Duas manchetes de status contraditórias na mesma página. Na execução `82394…` o header diz "hard gate failed" (`executionStatus` `:102-103`), mas o Decision diz "Resource Limit objectively; AI Fail" porque usa `primaryRun = runs[0]` (`:676`, `:290-301`) — um único run entre 6 — e o "What happened" narra só o timeout do Subagent Validation, ignorando os 2 hard gates (Policy Bound Action, Tool Contract Recovery) que são o resultado bloqueante (exec-hardgate-light-1440.png). | Decision agregado por execução ("2 hard gates failed · 1 resource limit · 3 passed", próximo passo = o do cenário mais grave) e a narrativa de IA por cenário dentro da linha do cenário. |
| ED-04 | P1 | copy | O título do Decision é a concatenação `${titleCase(systemStatus)} objectively; AI ${titleCase(advisoryStatus)}` (`:298-301`): "judge error objectively; ai inconclusive", "cancelled objectively; ai unavailable", "resource limit objectively; ai fail" (headings nos `.json` das capturas). Não é uma frase; "objectively" não modifica nada. | "objective result: judge error · advisory: inconclusive" como dois campos rotulados; para cancelled/running: "cancelled after 12m — no assessment". |
| ED-05 | P1 | redundância | O mesmo status aparece 7 vezes visíveis na primeira dobra da execução com falha: badge do header ("failed"), H2 do Decision, badge "system: judge error" (`:329-332`), texto "authoritative system status judge_error", tile "Objective system: judge error", tile "Effective harness: judge error" (igual ao objetivo nos 4 exemplos capturados; a própria caption diz que advisory "never overrides"), linha do cenário e tile "Objective" do run (crop-execfail-1440-decision.png). | Remover os badges duplicados do `PageHeader` do Decision (`:328-338`) e o tile "Effective harness" (`:389-393`), exibindo-o só quando diferir do objetivo. |
| ED-06 | P1 | densidade | 16 tiles numéricos antes do primeiro cenário (4 identidade + 8 MetricCards + 4 contadores). Vários sem informação: "Workflow scenarios 0/2 · No workflow steps retained · standard" (`:825-840`) aparece em 100% das execuções standard; "Reported cost — · usd"; "Assessments 0 / Passed 0 / Partial 0" (`:397-413`) na execução com falha; na cancelada 8x "—" + 6x "Not reported" (sonda). Os "deltas" são palavras de categoria ("usage", "efficiency", "tools", "usd", "wall clock", "standard", `:846-880`) sem função (crop-execfail-1440-top.png). | 4 tiles (scenarios passed, runtime, tokens, cost), esconder tiles sem dado, `delta` só para sinal real (needs attention / complete); o resto em `<details>` "more metrics". |
| ED-07 | P1 | IA / navegação | O roteador aceita as âncoras `/summary`, `/results`, `/assessments`, `/scenarios`, `/evidence`, `/raw-data`, `/technical`, `/configuration` (`:50-60`) e o estado `section` existe (`:542-548`), mas nenhuma UI o expõe: não há tabs nem índice; só o botão quiet "inspect retained evidence" (`:367-376`) e o skip-link levam a `/results`. Página de 2.8k–5.3k px sem sumário; o único "voltar" além do breadcrumb (escondido pelo ED-01) é o footer no fim. | Barra sticky de seções (summary · results · provenance) abaixo do header, sincronizada com a âncora da URL; "back to executions" no header. |
| ED-08 | P1 | A11Y | As três `<section aria-labelledby>` apontam para ids inexistentes — `summary-heading`, `results-heading`, `technical-heading` (`:314`, `:443`, `:490`): `PageHeader` nunca atribui `id` ao heading (`design-system/primitives.tsx:141-165`); sonda: `exists: false` x3. As regiões ficam sem nome no leitor de tela. | Prop `headingId` no `PageHeader` (ou `aria-label` textual nas sections). |
| ED-09 | P1 | A11Y contraste / tamanho | ~80 nós de texto abaixo de 11px (sonda: 25x 9.28px, 19x 9.6px, 12x 9.92px, 9x 9.76px, 2x 9.44px…), todos em `#a3a09c` = 2.49:1 sobre `#fafafa` e 2.39:1 sobre `#f7f5f2`: labels de identidade `text-[0.6rem]` (`:151`), provider `text-[0.59rem]` (`:167`), `DecisionBoundary` `text-[0.62rem]` (`:265`), provenance `text-[0.6rem]` (`:505`), breadcrumbs `text-[0.64rem]` (`:709`). | Piso 11px (`0.6875rem`, o próprio token de label do DS) e `--color-ink-faint` para tudo que é lido; ghost só em decorativo. |
| ED-10 | P1 | vocabulário visual | A página mais "bordada" do app: 38 caixas com borda sólida >= 1px e 16 usos de `rounded-lg` (8px) medidos (`:341`, `:397`, `:503`, `:516`); grid de decisão com `border` + linhas por `gap-px bg-[var(--color-rule)]`; 9 cards de provenance com borda; tiles de contagem com borda — contra "hierarquia por preenchimento, raio único 6px". No escuro a borda `#ffffff12` sobre `#111` = 1.19:1 e as caixas quase somem (exec-failed-dark-1440.png, seção provenance). | Substituir bordas por `--surface-fill` / `bg-panel-raised` aninhados e `rounded-[var(--ds-radius-md)]`; separar tiles por `gap`, não por `gap-px` colorido. |
| ED-11 | P1 | copy / estados | Execução cancelada: H2 "cancelled objectively; ai unavailable", "Recommended next step: Inspect the retained execution evidence before deciding whether to rerun." (fallback `:302-304`) e botão "inspect retained evidence" que leva a `#results`, onde se lê "No scenario reports were retained for this execution." (exec-cancelled-light-1440.png). Header: "— of — expected reports received." (`:722`). | Para cancelled / incomplete / running renderizar um único estado ("cancelled by layon after 12m 46s · no report retained · [re-run] [view log]") e esconder Decision, métricas e results. |
| ED-12 | P1 | estados | Running / cancelling não têm desenho: a página busca uma vez (`:559-583`) sem polling nem `subscribeRunChanges`; para "running" o presenter devolve `availability: 'unavailable'` (`presenter.rs:48-67`), então a página mostraria "Running" + "— of —" + 8 tiles "—" + Decision "running objectively; ai unavailable" + "Inspect the retained execution evidence before deciding whether to rerun". Sem log ao vivo, progresso (cenários concluídos/total), tempo decorrido nem botão Cancel (existe só em `components/LocalRunnerDialog.tsx:314`). Derivado de código — não havia execução em andamento para capturar. | Estado "live" com progresso "3/6 scenarios · 4m 12s", stream de eventos, Cancel com confirmação; ao terminar, substituir pelo relatório sem reload. |
| ED-13 | P1 | hierarquia / copy | H1 = `label` (`:721`), que para runs locais é o glob da CLI "e2e::* control-plane run" (17 das 20 execuções têm o mesmo título; executions-light-1440.png). O breadcrumb "Overview / Executions / e2e::* control-plane run" tampouco distingue; a identidade real (subject, data, resultado) está espalhada nos tiles. | H1 = "<subject> · <data>" ou label editável (Plans já tem rename); mostrar o glob como eyebrow/monospace secundário. |
| ED-14 | P1 | IA / ações | A página não tem ações: `DashboardPageActions` sem `actions` (`:703`) faz o cabeçalho do console perder "new plan / quick run" que a lista tinha (sonda: `consoleHeaderText` sem ações no detalhe). Não há re-run, cancel, copy link, download JSON, comparar com outra execução (rota `compare` existe, `hooks/use-hash-route.ts:119-126`) nem ir ao teste (rota `tests/<id>` existe). | Ações do header: "re-run", "compare", "copy link", "download report"; linhas de cenário com link para `tests/<id>`. |
| ED-15 | P1 | densidade / copy | Provenance expõe 9 cards com JSON cru: `source` → `{"ref":null,"repository":"iii-hq/harness-e2e","sha":"…"}`, `release` → 7 chaves `null` (`:509-511` `JSON.stringify`); timestamps ISO UTC "2026-08-26T20:07:31.012Z" enquanto o header usa local "Aug 26, 2026, 5:11 PM" (`execution-view.ts:295-304`) — dois fusos na mesma página; subtítulo "…without carrying the first-read experience." (`:496`) é linguagem de designer (crop-execfail-1440-tech.png). "Preview raw JSON" (`:516-523`) abre um `<pre>` de 42.880px (falha) / 324.256px (hard gate) dentro de 560px, sem copiar nem baixar. | `<dl>` compacto de 2 colunas (id, run id, sha curto com link, started/completed locais + duração, actor, event), esconder chaves `null`, botões "copy JSON" / "download". |
| ED-16 | P1 | A11Y | Skip-link "Skip to execution details" tem `href` de rota (`…/execution/<id>/results`, `:700-702`): Enter muda a URL (sonda: hash ganha `/results`), não move o foco (activeElement continua o próprio link) e o scroll não muda. | Mesmo fix do E-15: `preventDefault` + `focus()` no `#main`. |
| ED-17 | P2 | A11Y headings (cross-ref componentes) | Ordem H1 → H2 (Decision) → H2 (Scenario results) → H4 "Local Calc Smoke" (ScenarioMatrix/AssessmentWorkspace, salta o H3) → H2 (Raw fields) (headings em exec-failed-light-1440.json). | H3 para o nome do cenário dentro de Results. |
| ED-18 | P2 | responsivo | Em 390 os `MetricCard` ficam em 2 colunas de ~170px e label + delta quebram em 2 linhas cada ("OBJECTIVE / SCENARIOS" + "needs / attention"), colidindo visualmente; o delta em vermelho fica mais alto que o valor (crop-execfail-390-top.png). A página chega a 5.3k px. | Em < 640px: 1 coluna ou esconder o delta; `flex-wrap` com o delta abaixo do label; barra de seções sticky. |
| ED-19 | P2 | copy | Subtítulo "Objective outcomes remain authoritative; advisory AI is shown separately." repetido em todas as execuções (`:722`); captions "Deterministic gates and execution outcome." / "Qualitative guidance; never overrides the system." / "Canonical final status retained in the report." (`:379-393`) explicam o contrato de avaliação em todo carregamento — filosofia, não informação. | Um único tooltip "?" sobre o contrato (objective / advisory / effective); subtítulo vira dados: "2 scenarios · 1 failed · 4m 01s · deepseek-v4-flash judged by gpt-5.6-sol". |
| ED-20 | P2 | consistência | "SCENARIOS: 2 results" (label e valor com termos diferentes, `:754-760`); "Subject GPT-5.6 Terra" (`friendlyModelName` `:131-140`) vs "openai-codex/codex/gpt-5.6-terra" na lista; badge "Full evidence" (6px) vs "Diagnostic detail" (`rounded-full`) para o mesmo atributo; footer "Harness E2E · public execution report" (`:909`) num console interno. | Unificar termos (scenarios / evidence / model name) via helpers compartilhados; footer "execution report · copy link". |
| ED-21 | P2 | feedback (cross-ref componentes) | Num cenário expandido, "Open chat" aparece duas vezes (cabeçalho do run em y=547 e rodapé em y=1217, sonda); "Review evidence" só faz `setSelectedRunKey` do run já aberto (`components/AssessmentWorkspace.tsx:1106`, `:1211`); "Transcript" abre o diálogo. Os filtros "All · 0 / Failed · 0 / Low confidence · 0 / Unavailable · 0 / Asset-related · 0 / AI-evaluated · 0" (`AssessmentWorkspace.tsx:32-35`) são renderizados sobre um conjunto vazio (Assessments 0) (exec-failed-light-1440.png). | Um só "Open chat"; esconder os filtros quando total = 0; renomear "Review evidence" para o que faz ("show assessments") ou remover. |
| ED-22 | P2 | estados | Loading é a frase "Loading execution report…" num painel (`:657-674`), sem skeleton e sem o nav (o `DashboardPageActions` só é montado no estado final, `:703`); qualquer erro, rede inclusa, vira "Execution not found" com a mensagem técnica crua (`:641-643`). | Skeleton com header + 3 seções; distinguir 404 de falha de rede, com Retry. |
| ED-23 | P2 | navegação | Breadcrumb "Overview / Executions / <label>" (`:712-716`) sugere que Executions está sob Overview, mas no nav são abas irmãs; links em `#6b6865` sem sublinhado e 10.24px (sonda). No console, o cabeçalho continua "Executions · evidence, plans and live evaluation control", sem a identidade da execução. | "executions / <label>" em 12px; passar o label da execução ao cabeçalho do console via `setHeader`. |

### Oportunidades de redesenho
- Header de identidade em uma faixa: status + label + subject / judge + data + duração + tokens + custo numa linha de 56px, com ações (re-run, compare, copy link, download) à direita; elimina os 4 cards de identidade e metade dos tiles.
- Decision como "verdict card" agregado por execução (contagem por categoria + próximo passo do cenário mais grave), com a narrativa de IA movida para dentro de cada linha de cenário; sem auto-scroll.
- Barra de seções sticky (summary · results · provenance) ligada às âncoras já suportadas pelo roteador; Results vira a seção dominante da página.
- Estados dedicados para running / cancelling / cancelled / incomplete: um único bloco "live" com progresso e Cancel, ou "cancelled · no report" com re-run — sem tiles vazios nem Decision.
- Provenance como aside colapsável (ou drawer "raw") com `<dl>` limpo, sha com link e copy/download do JSON.

### Quick wins (só CSS/copy, < 1h cada)
- Remover o `useEffect` de auto-scroll (`ExecutionPage.tsx:612-623`).
- `:786-787`: tone `unavailable` e delta "not run" quando `scenarioCount === 0`.
- Título do Decision: "objective: judge error · advisory: inconclusive"; para cancelled: "cancelled · no assessment".
- Atribuir ids aos headings (`summary-heading`, `results-heading`, `technical-heading`) ou trocar por `aria-label`.
- Piso 11px nos labels: `0.59rem / 0.6rem / 0.62rem / 0.64rem` → `0.6875rem`, cor `--color-ink-faint`.
- `rounded-lg` → `rounded-[var(--ds-radius-md)]`; bordas → `bg-panel-raised`.
- Provenance: `formatDate` + duração nas datas, esconder chaves `null`, botão "copy".
- Footer: "public execution report" → "execution report"; adicionar "copy link".
- Esconder "Workflow scenarios" e "Reported cost" quando sem dado; remover deltas de categoria ("usage", "tools", "usd").
- Skip-link com `preventDefault` + `focus()`.
- Um fetch em vez de dois (`summaryFromDetail` já existe, `:62-84`).
