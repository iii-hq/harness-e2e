## Shell / navegação (rota `#/ext/harness-e2e/*`)

### Anatomia atual
- Header do console (`PageHeader` de `@iii-dev/console-ui`, montado em `components/DashboardShell.tsx:228-244`): ícone próprio (`HarnessE2eIcon`, :162-174), título "harness e2e", descrição `"${Seção} · evidence, plans and live evaluation control"` (:231), ações injetadas pelas páginas via `AppHeader`/`DashboardPageActions` (`components/AppHeader.tsx:48-56`, classe em :23 = 28px de altura, mono 12px) e "×" (`onClose`).
- Barra de seções (`DashboardShell.tsx:253-295`): `sticky top-0 z-10 border-b` com `Tabs/TabsList/TabsTrigger` do console (Overview / Tests / Executions / Plans, ícones SVG desenhados à mão :109-144); abaixo de 720px de **container** (`hooks/use-container-narrow.ts`, ResizeObserver) deveria trocar por um `<Select>` (:286-294, `dashboard-shell.css:145-151`). Coverage fica fora do menu (:146-152).
- Conteúdo: `.harness-e2e-dashboard min-w-0 overflow-x-hidden` (:249) → `<RoutedPage>` (`App.tsx:14-36`) troca a árvore inteira por rota; `hooks/use-hash-route.ts:197-215` ouve `hashchange` e, **só em standalone**, faz `window.location.reload()` ao mudar de página (:204-210).
- Cada página desenha o seu próprio "cromo" legado por cima: `skip-link`, `.ambient` (display:none), `<main class="page-shell …">`, eyebrow com `.live-dot`, h1 lowercase, `<footer>` "Harness E2E · … / Back to …" (ex.: `PlansPage.tsx:419-455, 611-613`; `ExecutionPage.tsx:700-716, 908-914`; `CoveragePage.tsx:11-38`).
- Tema: embutido recebe `theme` por prop (`App.tsx:42`, `DashboardShell.tsx:192-198`); standalone usa `useTheme` + bootstrap inline em `index.html:12-17` (sem flash). `ThemeToggle` só aparece em standalone (:234-239).

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| S-01 | P0 | navegação / responsivo | **Abaixo de 720px de container a navegação desaparece por completo.** O `<Select>` de fallback tem a classe Tailwind `hidden` (`DashboardShell.tsx:286`) e `index.css:7` importa Tailwind com `important` → no bundle `dist/assets/*.css` sai `.hidden{display:none!important}` enquanto o override é `.harness-e2e-navigation-narrow{display:block}` sem `!important` (`dashboard-shell.css:149-151`). Resultado em todas as capturas estreitas: `overview-light-720.png`, `overview-light-390.png`, `executions-light-720/390.png`, `plans-light-720/390.png`, `plan-new-light-720/390.png`, `exec-failed-light-720/390.png` — nenhuma tem tabs nem select; o único caminho entre seções são as ações do header ou editar a URL. | Tirar `hidden` do JSX e controlar os dois estados apenas em `dashboard-shell.css` (`.harness-e2e-navigation-narrow{display:none}` / `[data-narrow="true"] … {display:block}`), ou remover o `important` do import Tailwind (ver DS-14). Adicionar teste de visibilidade em 600px ao `DashboardShell`. |
| S-02 | P0 | navegação / responsivo | A 390px as ações do header empurram o "×" para fora da tela na seção Tests: `x08-tests-390-header.png` mostra "harness e2e · +new test · new plan · compare" e nenhum botão de fechar (compare com `x08-exec-390-header.png`, onde o × existe). Causa: três ações `shrink-0` (`AppHeader.tsx:23`) num `flex … gap-2` (`DashboardShell.tsx:233`). | Em `narrow`, renderizar só a ação primária + menu "⋯" (overflow) e garantir `min-w-0` nas ações; o `×` do console nunca pode sair do viewport. |
| S-03 | P1 | hierarquia | Dois níveis de cromo (header 44px + tabs 52px) antes do conteúdo e a seção aparece duas vezes (descrição "Overview · evidence, plans and live evaluation control" e tab ativa). O sufixo é slogan, não orientação; a 390 vira "Executions · evidence, plans…" / "harness e2e ‹" (`crop-ov-390-top.png`, `x08-exec-390-header.png`). | Descrição = contexto vivo ("Executions · 20 runs · last Aug 26", "Plans · system-prompt validation") ou vazia; tirar o slogan de `DashboardShell.tsx:231`. |
| S-04 | P1 | ações / descobribilidade | As ações do header mudam por página sem padrão visível: Overview/Executions = "new plan", "quick run" (`OverviewPage.tsx:672-681`); Tests = "+new test", "new plan", "compare" (`TestsCatalogPage.tsx:374-377`); Plans = "new plan" (`PlansPage.tsx:231-238`); Compare = "new run" (`TestsPage.tsx:794-802`); Plan detail/new, Test history e Coverage = nada. "quick run" (header) e "run suite" (aside) abrem o mesmo diálogo (`OverviewPage.tsx:634-640, 691, 749`). Todas com o mesmo estilo de link (mono 12px, 28px, sem ícone) — a "primary" (`harness-e2e-header-action-primary`) não é usada em nenhuma captura. | Uma ação primária por seção no header (Overview/Executions: "run"; Tests: "+ new test"; Plans: "+ new plan"), em estilo tinta; secundárias ("compare", "new plan" fora de Plans) vão para o conteúdo. |
| S-05 | P1 | semântica / navegação | As seções são `<button role="tab">` que mudam o hash (`DashboardShell.tsx:216-218, 270-282`): não são links (sem abrir em nova aba, sem copiar URL, sem `aria-current`); o JSON lista "Overview/Tests/Executions/Plans" em `buttons`. Semântica de tabs pressupõe conteúdo trocado in-place, não páginas com URL própria. | `<nav aria-label="Harness E2E sections">` com `<a href={hashForSection()} aria-current="page">`, mantendo o visual de tabs do console. |
| S-06 | P1 | navegação | Na rota Coverage nenhuma tab fica selecionada (`coverage-light-1440.png`: sem sublinhado) porque `Tabs value="coverage"` não tem trigger (:146-152); no fallback narrow o `<Select value="coverage">` também não tem opção → mostraria "Overview". | Ou incluir Coverage como tab/sub-rota de Overview com breadcrumb, ou remover a rota (ver CV-03). |
| S-07 | P1 | UX / estado | Troca de página em standalone = `window.location.reload()` (`use-hash-route.ts:204-210`): flash (html pinta `#0a0c0b` antes do React, `index.css:38, 82-93`), perda de filtros/scroll/diálogos, re-download do bundle. Em modo embutido não recarrega, mas `RoutedPage` desmonta tudo e ninguém chama `scrollTo` (grep vazio): o container `.harness-e2e-console-main` (overflow:auto, :246) mantém o scroll — sair de Plans (3.792px) rolado ao fundo pode aterrissar no meio de Overview. | Remover o reload (as páginas já são componentes React); no `hashchange` fazer `mainRef.current.scrollTo(0,0)` salvo quando há anchor; persistir filtros na query (`?status=failed`). |
| S-08 | P1 | arquitetura de informação | Overview e Executions são a mesma tabela (`OverviewPage` com `activeView`): as 20 linhas de `overview-light-1440.png` são as de `executions-light-1440.png`, que só acrescenta EFFICIENCY/EVIDENCE. Duas tabs, um conteúdo. | Overview = KPIs + última execução + 5 recentes + "ver todas"; Executions = ledger completo com paginação e as 7 colunas. |
| S-09 | P1 | consistência / "onde estou" | Quatro padrões de localização/voltar: eyebrow + h1 + footer "Back to overview" (Plans, `PlansPage.tsx:611-613`), breadcrumb "Overview / Executions / …" + footer "Back to all executions" (`ExecutionPage.tsx:708-716, 908-914`), breadcrumb "Tests / chess_play_ladder" sem footer (`TestHistoryPage.tsx:1182`), footer "Back to home" (plan new, `plan-new-light-1440.png`) e "Back to plans" (plan detail). Overview/Tests/Executions não têm h1. | Um único padrão: breadcrumb DS (contexto) + h1 da entidade; remover footers "Back to…" (a tab já é o voltar). |
| S-10 | P2 | cromo | A barra de tabs tem `border-b` de 1px em toda a largura (`DashboardShell.tsx:254`) mais o sublinhado curto do `TabsList` do console → duas linhas sobrepostas em `overview-light-1440.png` y≈152. Única borda 1px no cromo, contra "hierarquia por preenchimento". | Remover `border-b border-line`; se precisar separar, usar `bg-panel-raised` na barra. |
| S-11 | P2 | iconografia | Ícones das tabs desenhados à mão (`DashboardShell.tsx:109-144`: frasco para Tests, relógio para Executions, "nós" para Plans, relógio de novo para Coverage) enquanto o resto usa lucide (`ThemeToggle.tsx:1`, `LegacyLoadError.tsx:1`). Executions e Coverage têm o mesmo glifo. | Usar lucide (`LayoutDashboard`, `FlaskConical`, `History`, `GitCompare`) ou nenhum ícone. |
| S-12 | P2 | código morto | `components/SectionNav.tsx` (01/02/03 com `aria-current`) não é importado por ninguém; `.section-nav` ocupa 18 regras em `index.css:145-200`. | Apagar. |
| S-13 | P2 | tema (standalone) | `ThemeToggle.tsx:17-19` mostra o rótulo/ícone do tema **destino** ("Dark" + lua quando está claro) — padrão ambíguo. | Ícone do estado atual + `aria-pressed` ou `role="switch"` com `aria-checked`. |
| S-14 | P2 | título do documento | `App.tsx:56-69` gere `document.title` por rota, mas embutido o título é sempre "iii console" (todos os JSON) → histórico do browser sem distinção. | Expor o título por API do host (se existir) ou aceitar e documentar. |

### Oportunidades de redesenho
- **Um só cromo**: fundir a barra de tabs no header do console (título "harness e2e" à esquerda, seções como segmented links no centro, 1 ação primária + "⋯" à direita). Poupa 52px por página e resolve S-01/S-02/S-03 de uma vez.
- **Overview como painel, Executions como ledger** (S-08): Overview = última execução + 4 KPIs + alerta + 5 recentes; a tabela completa com filtros vive só em Executions.
- **Navegação por links + breadcrumb DS**: `<nav>` com `<a aria-current>`; breadcrumb único para detalhe (Executions / e2e::* control-plane run), sem footers.
- **Ações contextuais em um lugar**: header carrega só a primária da seção; ações de linha/página ficam no conteúdo.

### Quick wins
- `DashboardShell.tsx:286`: remover `hidden` e mover o toggle para CSS (S-01).
- `AppHeader.tsx:23`: `shrink-0` → `min-w-0 shrink` e `max-sm:hidden` nas ações secundárias (S-02).
- `DashboardShell.tsx:231`: descrição = `sectionLabel` apenas (S-03).
- `DashboardShell.tsx:254`: tirar `border-b border-line` (S-10).
- Apagar `SectionNav.tsx` e `index.css:145-200` (S-12).

---

## Design system (dívida DS × CSS legado)

### Anatomia atual
- **DS**: `design-system/foundations.css` (6 tamanhos de texto, espaços, raio 6px, `--shadow-panel:none`), `primitives.css` (`.ds-button` 36/28/44px mono 13px; `.ds-panel` fill sem borda; `.ds-status-badge` ponto 6px + texto; `.ds-page-header` h1 1rem; `.ds-metric-card` 1.375rem) e `primitives.tsx` (`Button`, `Panel`, `StatusBadge`, `PageHeader`, `MetricCard`). Ponte de tokens em `components/dashboard-shell.css:13-63` (mapa `--he-*` → `--surface/--text/--accent…`, raio 6px, sombra none).
- **Adoção real** (grep JSX, sem testes): `StatusBadge` 16 usos (OverviewPage, ExecutionPage, SemanticTestFlow, ScenarioMatrix), `MetricCard` 9 (Overview, Execution), `Panel` 8 (idem), `PageHeader` 5 (Execution), `Button` 3 (Overview). **Zero** em PlansPage, LocalPlanPage (plan new/detail), TestsCatalogPage, TestsPage (compare), TestHistoryPage, CoveragePage, ExecutionSetup, LocalScenarioEditor, LocalRunnerDialog, TranscriptDialog, AssessmentWorkspace.
- **Legado**: `index.css` com 9.696 linhas: `.plan-*` 393 linhas de seletor, `.overview-*` 78, `.scenario*` 73, `.compare*` 37, `.execution-*` 32, `.run-*` 29, `.history*` 23, `.section-nav` 18 (morto), `.tmh-*` (test history com tokens `--tmh-*`, :4074-4100). Classes legadas por arquivo: LocalPlanPage 33, TestsPage 20, PlansPage 20, ExecutionPage 15, AssessmentWorkspace 13, SemanticTestFlow 11, ScenarioMatrix 10, LocalScenarioEditor 9.
- **Tailwind v4** importado com `important` (`index.css:7`) e `@theme inline` (:9-34) mapeando `--color-*` para os tokens legados; utilities arbitrárias em TSX (`text-[0.62rem]` ×35, `max-[560px]:` ×17…).
- **Paleta legada** `:root` (`index.css:36-72`): escura, neon (`--accent #c7ff4a`, `--danger #ff786f`, `--success #9ee66c`, `--warning #ffd166`, `--accent-blue #7bc7ff`, `--code-bg #080a09`). Dentro do shell é sobreposta pela classe dupla (`dashboard-shell.css:13-14`), exceto `--code-bg/--code-text` (não remapeados) e os 63 literais `rgba()` derivados dela.

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| DS-01 | P0 | adoção | Só 2 páginas (Overview, Execution detail) usam as primitivas; Plans, Plan new/detail, Tests catalog, Compare, Test history, Coverage e todos os diálogos reimplementam painel/botão/métrica/cabeçalho com `.plan-*`, `.button`, `.panel`, `.tmh-*` ou Tailwind ad hoc (contagens acima). Visível lado a lado: `overview-light-1440.png` (fill, sem borda) vs `plans-light-1440.png` (cartões brancos com borda 1px sobre fundo cinza — hierarquia invertida) vs `exec-failed-light-1440.png` (cartões com borda) vs `testhist-chess-light-1440.png` (bordas + fundo raised). | Ordem de migração por impacto: PlansPage → LocalPlanPage → ExecutionSetup/dialogs → TestsCatalogPage → TestsPage → TestHistoryPage. Regra de PR: nenhum `border: 1px solid` novo fora de `.ds-*`. |
| DS-02 | P0 | vocabulário (borda/raio/sombra) | `index.css` tem **204** `border(-*): 1px`, **17** valores de `border-radius` (6px só 22 de ~143 usos: 8px ×21, 7px ×15, 10px ×10, 9px ×8, 999px ×8, 50% ×7, 4px ×6, 5px ×5, 12px/11px ×3, 3px, 2px), **15** `box-shadow` reais (`:1498` e `:3151` `0 32px 100px rgba(0,0,0,.65)`, `:5428`, `:6339`, `:1938`, `filter: drop-shadow` `:869`) e **13** `!important` (`:7589-7603` tons de métrica, `:8572-8575`). Ex.: `.plan-list-card` `border:1px solid var(--line-strong)` (`:7363`), `.raw-details pre` `border-radius:8px` (`:3018`). | Tokens únicos: `--radius:6px`, `--shadow:none`, `--shadow-dialog` (um só). Stylelint: `declaration-property-value-disallowed-list` para `border-radius` ≠ `var(--radius-*)` e `box-shadow` ≠ none/var. |
| DS-03 | P1 | cor / paleta legada | A paleta neon `:root` ainda alimenta 63 literais `rgba` que não seguem tema nem console: salmão `rgba(255,120,111,…)` em `.detail-alert` `:2353-2355`, `.failure-chip` `:2407-2416`, `.conversation-tool-error` `:3543-3574`, `.matrix-failed` `:1970-1971`; menta `rgba(158,230,108,…)` `:1379-1381`, `:1961-1962`; âmbar `rgba(255,209,102,…)` `:327-328`, `:913`, `:1387-1388`, `:1982-1983`, `:2159`; azul-bebê `rgba(123,199,255,…)` `:1976-1977`, `:2040-2042`, `:3062-3064`. No tema claro do console (`#ff0026/#356f3d/#a87a00/#b8420f`) são tintas de outra família. `--code-bg #080a09`/`--code-text` (`:56-57`) nunca são remapeados em `dashboard-shell.css` e `.raw-details pre` (`:3019`) ainda os usa (não aparece nas capturas — mina terrestre). | Substituir todos por `color-mix(in srgb, var(--danger|--success|--warning|--accent) N%, transparent)`; apagar `--code-*` e usar `--surface-raised`. |
| DS-04 | P1 | tipografia (famílias) | A sans renderizada no console **não é Inter**: o stack começa por `-apple-system` em 61/61 JSON (`fonts`), porque `.harness-e2e-shell` usa `var(--font-sans, Inter…)` (`dashboard-shell.css:62`) e o host define `--font-sans`. Os `@fontsource/inter` (`index.css:1-3`) são baixados e não usados. Test history adiciona `Inter` explícito e `"SFMono-Regular", Consolas` (`index.css:4987, 5089, 5551`) → 4 famílias numa sessão (`testhist-*.json`: `-apple-system, Chivo Mono, Inter, SFMono-Regular`). | Duas famílias, ambas via token do host: `var(--font-sans)` e `var(--font-mono)`; remover `@fontsource/inter` se o console já serve a sans; trocar SFMono por `var(--font-mono)`. |
| DS-05 | P1 | tipografia (escala) | **44** tamanhos computados distintos nas capturas (8px → 40px), 17 abaixo de 11px; `index.css` declara ~75 valores de `font-size` (0.5rem … 2.5rem, 8px … 22px, 6 `clamp()`); TSX adiciona 18 arbitrários (`text-[0.62rem]` ×35, `text-[0.58rem]` ×10, `text-[9px]`) + 6 nomeados (`text-xs` ×138). Por página: Overview 12, Tests 9, Executions 11, Plans 24, Plan detail 30, Plan new 30, Exec detail 19, diálogos 27, Test history 14, Coverage 10. O DS define 7. | Escala fechada abaixo ("Mapa de tipografia") + lint que proíbe `font-size` literal e `text-[…]`. |
| DS-06 | P1 | rótulos | 54 `text-transform: uppercase` com 10+ trackings (0.045/0.05/0.055/0.06/0.07/0.08em, -0.035em…); o DS pede 0.6875rem + 0.06em. Visível: "LATEST EXECUTION" vs "SCOPE LOCKED" vs "PLANS IN THIS DASHBOARD" vs "COMPARISON CONTEXT" em 4 tamanhos/trackings (`overview-light-1440.png`, `plans-light-1440.png`, `compare-light-1440-part1.png`). | Uma classe `.ds-label` (11px/0.06em/soft) e proibir uppercase fora dela. |
| DS-07 | P1 | badge | Badge "LOCAL" (`TestsCatalogPage.tsx:283-289`): `rounded-full border border-brand/25 bg-brand-soft text-[9px] font-sans uppercase tracking-[0.05em]` → 9px, pílula, com borda, sans, uppercase, cor accent (laranja claro / azul escuro, `crop-tests-local-badge.png`, `crop-tests-dark-local-badge.png`). Cinco desvios do vocabulário "ponto 6px + mono lowercase". Contraste 4.56:1 ok, mas 9px. | `StatusBadge`-like neutro: "● local" mono 12px em `--text-soft`, ou sufixo textual no id. |
| DS-08 | P1 | componentes duplicados | Stepper "01/02/03" existe em 3 versões: cartões com borda em Plans (`PlansPage.tsx:462-472`, `plans-light-1440.png`), números accent nos diálogos/plan new (`ExecutionSetup.tsx:214-325`, `dlg-runsuite-light-1440.png`) e `SectionNav` morto. | Um componente `Steps` (ou remover: em Plans o stepper é decorativo). |
| DS-09 | P1 | botões | Quatro implementações: `.ds-button` (36px/13px), `.button` legado (19 regras; "View metrics" ×47 em `tests-light-1440.png`; "Diagnostic detail" pílula em `executions-light-1440.png`), header actions (28px/12px, `AppHeader.tsx:23`) e Tailwind nos diálogos ("Refresh", "Select visible (47)", "Create test" **laranja preenchido** em `dlg-newtest-light-1440.png`, "Run selected E2E" laranja/azul em `dlg-runsuite-*`). O DS diz primary = tinta; os diálogos usam accent. | Só `Button` do DS; primary sempre tinta; accent reservado a links/foco. |
| DS-10 | P1 | métricas | Quatro variantes do "cartão de métrica": `MetricCard` (fill), `.latest-kpi` (fill, Overview), `.plan-*` KPIs com borda (`index.css:7363`, `plans-light-1440.png` "NEEDS ACTION 6"), exec detail com borda (`exec-failed-light-1440.png`), `tmh-*` (`testhist-chess-light-1440.png`). Tamanhos do valor: 22px, 30.4px, 20px, 18px. | `MetricCard` único com `tone` e `size`. |
| DS-11 | P2 | headings | `:where(h1,h2,h3,h4)` força mono lowercase (`dashboard-shell.css:121-126`) mas páginas sobrescrevem: "Execution history" (sans, `plan-sysprompt-light-1440.png`), "Create a quick benchmark result" (sans bold, `dlg-runsuite-light-1440.png`), "Judge model needs investigation" (`<strong>` sans) vs "choose a plan to continue"/"metric history" (mono lowercase). | h1/h2 = mono lowercase 1rem; h3+ = sans 0.875rem semibold; nada mais. |
| DS-12 | P2 | breakpoints | 11 media queries diferentes em `index.css` (1120, 960, 900, 840, 760, 720, 620, 560, 500, 430) + Tailwind `sm/md/lg/xl` (64/29/61/2 usos) + `max-[560px]` ×17, `max-[640px]`, `max-[460px]`, `max-[390px]` — todos por **viewport** — enquanto o shell decide `narrow` por **container** (`use-container-narrow.ts`, 720). Num split-pane do console o dashboard pode ter 700px numa janela de 1440 e continuar em layout desktop. | `@container` queries (`container-type: inline-size` no `.harness-e2e-dashboard`) com 2 pontos: `narrow` (≤720) e `compact` (≤480); propagar `data-narrow` para as páginas. |
| DS-13 | P2 | sombras | `--shadow:none` (`dashboard-shell.css:55`) mas diálogos usam `0 32px 100px rgba(0,0,0,.65)` (`index.css:1498, 3151`) e há `inset 3px` como "borda" (`:3545`). | `--shadow-dialog: 0 24px 64px rgb(0 0 0/.25)` único; `inset` → `border-left`. |
| DS-14 | P2 | build | `@import "tailwindcss" important` (`index.css:7`) torna toda utility `!important`: causa raiz de S-01, impede overrides por tema/estado e força mais `!important` no legado (`:7589-7603`). | Remover `important`; resolver conflitos com `@layer base/components/utilities` e especificidade normal. |

### Mapa de tipografia (renderizado hoje → escala proposta)
- Hoje: famílias `-apple-system` (sans do host), `"Chivo Mono"`, `Inter` (só test history), `SFMono-Regular` (só test history). Tamanhos: 8 · 8.64 · 8.8 · 8.96 · 9 · 9.12 · 9.28 · 9.44 · 9.6 · 9.76 · 9.92 · 10 · 10.08 · 10.24 · 10.4 · 10.56 · 10.88 · **11** · 11.04 · 11.2 · 11.36 · 11.52 · 11.84 · **12** · 12.16 · 12.48 · **13** · 13.12 · 13.76 · **14** · 14.08 · 15.04 · **16** · 17.6 · 18 · 19 · 19.2 · 20 · 21 · 21.6 · **22** · 24 · 30.4 · 40 (px).
- Proposta (6 passos, 2 famílias):

| Token | Tamanho | Família | Uso |
|---|---|---|---|
| `--ds-text-label` | 11px / 0.6875rem | mono, uppercase 0.06em, cor **soft** | th, eyebrow — único uppercase permitido |
| `--ds-text-xs` | 12px / 0.75rem | mono | dados de tabela, status, meta, datas, ids |
| `--ds-text-sm` | 13px / 0.8125rem | mono (botões) / sans (corpo) | botões, corpo denso |
| `--ds-text-md` | 14px / 0.875rem | sans | descrições, alertas, formulários |
| `--ds-text-lg` | 16px / 1rem | mono lowercase 600 | h1/h2 |
| `--ds-text-display` | 22px / 1.375rem | mono 600 tabular | valor de métrica |

- Piso: nada abaixo de 11px; 9.28px (`0.58rem`, 21 declarações), 8.96px (`0.56rem`) e `text-[9px]` desaparecem.

### Oportunidades de redesenho
- **Migração por página para as 5 primitivas** com "orçamento de dívida": cada PR em `pages/*` remove ≥ 1 bloco `.plan-*/.tmh-*` de `index.css`; meta: `index.css` < 3.000 linhas.
- **`Table` primitiva** (th 11px soft, td 12px mono, linha 40px, colapso por container) usada por Overview/Executions/Tests/Test history/Compare/Plan detail — hoje cada uma reimplementa.
- **`Dialog` primitiva** (uma sombra, uma largura, sheet em narrow) para ExecutionSetup, LocalScenarioEditor, LocalRunnerDialog, TranscriptDialog.
- **Fechar a paleta**: apagar o `:root` neon; o standalone passa a declarar os mesmos tokens do console (já existe em `dashboard-shell.css:73-109`).

### Quick wins
- `primitives.css:5-24`: `white-space: nowrap` no `.ds-button` (evita "open / plan →").
- Substituir `text-[0.58rem]`/`text-[0.6rem]`/`text-[0.61rem]`/`text-[0.62rem]` por `text-[0.6875rem]` via sed (66 ocorrências).
- `TestsCatalogPage.tsx:283-289`: badge LOCAL → "· local" mono 12px.
- `dashboard-shell.css`: adicionar `--code-bg: var(--surface-raised); --code-text: var(--text-soft)`.
- Apagar `.section-nav`, `.ambient`, `.badge-preview` (sem uso).

---

## Tema escuro (DK)

### Anatomia atual
- Embutido: o console passa `theme` → `data-theme` no shell e no dashboard (`DashboardShell.tsx:225, 251`); `dashboard-shell.css:65-71` só troca `--he-ink/--he-panel/--contrast-rgb`; tudo o resto encadeia nos `--color-*` do host. Standalone: tokens próprios (`:73-109`) + bootstrap inline (`index.html:12-17`) → **sem flash de tema** em nenhum dos modos (verificado no código; nas capturas dark não há resíduo claro).
- Nas 9 capturas dark (`overview`, `tests`, `executions`, `plans`, `exec-failed`, `plan-sysprompt`, `plan-new`, `testhist-chess`, `dlg-runsuite`) **nada fica invisível**: a ponte de tokens funciona. Os problemas são de contraste-limite, de cores hardcoded fora da ponte e de semântica de cor que muda de matiz entre temas.

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| DK-01 | P1 | contraste | `--text-muted` dark `#6f6f6f` sobre `#111111` = **3.76:1** e sobre fill `#1e1e1e` = **3.32:1** — abaixo de 4.5:1 para os cabeçalhos de coluna (11px), eyebrows ("LATEST EXECUTION", "QUICK RUN", "CATALOG"), rótulos de métrica, datas de linha (10.4px) em `overview-dark-1440.png`, `executions-dark-1440.png`, `plans-dark-1440.png`, `plan-new-dark-1440.png` ("TESTS / LOGICAL RUNS"), `dlg-runsuite-dark-1440.png`, `testhist-chess-dark-1440.png`. | `--color-ink-ghost` dark ≥ `#8a8a8a` (5.47:1 / 4.83:1) para texto; manter `#6f6f6f` só para decoração. |
| DK-02 | P1 | primária inconsistente | Primary = `--text` (`dashboard-shell.css:132-135`): em dark vira botão **branco** ("run suite", "run candidate", "new local plan" em `overview-dark`, `plan-sysprompt-dark`, `plans-dark`), mas nos diálogos/plan new o CTA é **azul** `#28a8f7` ("Run selected E2E" `dlg-runsuite-dark-1440.png`, "Create draft plan" `plan-new-dark-1440.png`) e em light é laranja preenchido (`dlg-newtest-light-1440.png` "Create test"). Três primárias por tema. | Uma primária (tinta/`--text`) em todos; accent só em links e foco. |
| DK-03 | P1 | não-texto | Campos sem borda dependem só do fill: `rgba(255,255,255,.055)` sobre `#111` = **1.13:1** (light 1.12:1). Em `overview-dark-1440.png` a caixa "Search label, run, or date" e os selects são quase invisíveis; nos diálogos a borda `--line` `rgba(255,255,255,.12)` ≈ 1.3:1. WCAG 1.4.11 pede 3:1 para o limite de controles. | Inputs/selects com borda 1px em `rgba(255,255,255,.4)` (dark) / `rgba(20,16,8,.5)` (light ≈ `#878582`, 3.53:1) ou fill `.12` + borda. |
| DK-04 | P1 | cores hardcoded | Os `rgba(255,120,111,…)` (salmão), `rgba(158,230,108,…)`, `rgba(255,209,102,…)`, `rgba(123,199,255,…)` de DS-03 não trocam com o tema: em dark convivem com `#f05d68/#36c98f/#f5a524/#28a8f7` do console (dois vermelhos, dois verdes, dois azuis); em light são tintas frias sobre fundo quente. Locais: `.matrix-*` (ScenarioMatrix, compare), `.detail-alert`, `.failure-chip`, `.conversation-tool-error` (transcript). | `color-mix(var(--danger) 8%, transparent)` etc. — resolve os dois temas de uma vez. |
| DK-05 | P2 | semântica de matiz | O accent muda de laranja queimado (light) para azul (dark): o mesmo elemento ("EXECUTION SETUP", ponto do eyebrow "LOCAL COMPARISON WORKSPACE", "Browse tests →", badge LOCAL, números do stepper) muda de "quente/ação" para "frio/info". `--accent-blue` é alias de `--accent` (`dashboard-shell.css:41`), então "info" e "marca" colapsam; `.matrix-running` usa azul fixo `rgba(123,199,255)` (`index.css:1976-1978`) nos dois temas. | Tokens separados `--accent` (marca) e `--info` (running/links informativos), ambos do host; não usar accent para rótulos decorativos. |
| DK-06 | P2 | standalone | `html, body { background: var(--bg) }` com `--bg: #0a0c0b` (`index.css:38, 82-93`) independentemente do tema; `:root[data-theme="light"]` (`:6207-6220`) só muda `color-scheme`. Em standalone claro, overscroll/áreas fora do shell ficam quase pretas. (Não capturado — verificar em standalone.) | Mover `--bg` claro/escuro para `:root[data-theme]` ou pintar `html` com `var(--color-bg)` do shell. |
| DK-07 | P2 | alerta | Banner "Judge model needs investigation" usa `color-mix(var(--danger) 7%)` (`OverviewPage.tsx:351`) → rosa em light, vinho em dark (`crop-ov-dark-hero.png`) — correto, mas é o único alerta com esse tratamento; `.detail-alert` (`index.css:2353`) e "Primary concern" (`exec-failed-*`) usam salmão fixo. | Um `Callout tone="danger|warning|info"` no DS. |
| DK-08 | P2 | `color-scheme` | `:root { color-scheme: dark }` (`index.css:37`) é a base; o light só é restaurado por `:root[data-theme="light"]` (`:6208`). Se o scoper do console não reescrever esse seletor, `<select>` nativos e scrollbars ficam escuros no tema claro. Nas capturas light os selects estão claros (ok hoje), mas o acoplamento é frágil. | Declarar `color-scheme` no `.harness-e2e-shell[data-theme]` (`dashboard-shell.css`), não em `:root`. |

### Oportunidades de redesenho
- Tirar do dark a dependência de borda: onde Plans/Exec detail usam `1px rgba(255,255,255,.12)` (visível em `plans-dark-1440.png`) o DS usa fill — ao migrar (DS-01) o dark fica automaticamente consistente.
- Definir 4 tons semânticos com par light/dark **e** variante "texto" (contraste ≥ 4.5) + "tinta 8%": `danger`, `warning`, `success`, `info`.

### Quick wins
- `--color-ink-ghost` dark → `#8a8a8a`.
- Bordas de input em `dashboard-shell.css` (light `rgba(20,16,8,.5)`, dark `rgba(255,255,255,.4)`).
- Trocar as 4 famílias de `rgba(255,120,111|158,230,108|255,209,102|123,199,255, …)` por `color-mix` (sed em `index.css`, ~40 linhas).

---

## Responsivo 720 / 390 (RD)

### Anatomia atual
- O shell mede o **container** (`use-container-narrow.ts`, limiar 720) e expõe `narrow` via contexto (`DashboardShell.tsx:211`); as páginas ignoram o contexto e usam media queries de viewport (DS-12). Raiz com `min-width: 320px` + `overflow-x: hidden` (`console-overrides.css:16-18`, `DashboardShell.tsx:249`).
- Tabelas: `<table class="w-full min-w-[56rem]">` dentro de `overflow-x-auto` (`OverviewPage.tsx:454-455`, 896px) e `min-w-[82rem]` (`TestsCatalogPage.tsx:536-537`, 1312px); test history (`tmh-*`) colapsa para cartões (`index.css:9459+`). Plans é lista de cartões; Plan new/diálogos são 2 colunas (form + aside "review setup") que empilham.

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| RD-01 | P0 | navegação | = S-01: sem tabs nem select em **todas** as capturas de 720 e 390. | Ver S-01. |
| RD-02 | P0 | header | = S-02: "×" fora do viewport em Tests a 390 (`x08-tests-390-header.png`). | Ver S-02. |
| RD-03 | P1 | tabelas | A 720 as tabelas mostram 3 colunas e cortam a 4ª a meio: `executions-light-720.png` (EXECUTION / RESULT / "openai-codex/codex/gpt-" — SCOPE, OUTCOME, EFFICIENCY, EVIDENCE fora), `tests-light-720.png` (TEST / VERSION / LIFECYCLE / COMPLEXITY / "HUMAN HO"); a 390 restam 1-2 colunas (`executions-light-390.png` "RESU", `tests-light-390.png` só TEST + VERSION). O scroll horizontal existe mas sem affordance (scrollbar oculta, sem sombra de borda), e a única ação de linha ("View metrics", "Diagnostic detail") fica 900-1.300px à direita. | Regras gerais abaixo: colapsar colunas secundárias em metadados sob o nome (≤720) e em cartão (≤480); ação de linha sempre visível (coluna fixa à direita ou no cartão). |
| RD-04 | P1 | hierarquia | A 720/390 o aside "Quick run / Catalog" (com a primária "run suite") cai **depois** das 20 linhas: `overview-light-720.png` y≈1.630/2.208, `overview-light-390.png` y≈2.290/2.611. | Em narrow: aside vira faixa de ações no topo (ou a primária fica só no header, S-04). |
| RD-05 | P1 | filtros | A 720 o select "All triggers" não aparece (`executions-light-720.png`, `crop-exec-720-filters.png`: busca, "All statuses", cabeçalho da tabela), mas a 390 volta (`executions-light-390.png`). Grid `sm:col-span-2 md:col-span-1` (`OverviewPage.tsx:416-450`) entre 640 e 768px deixa o 3º item numa linha que não é renderizada/visível. Verificar overflow do grid. | Grid explícito: `grid-cols-1 sm:grid-cols-[1fr_auto_auto]`; a 390 filtros num `<details>` "Filters (2)". |
| RD-06 | P1 | botões | "Open plan →" quebra em duas linhas a 720 e 390 (`plans-light-720.png`, `plans-light-390.png`) — `PlansPage.tsx:286` com botão legado sem `white-space: nowrap`. Botões do aside a 390 ("Set A", ícone de chat) ficam empilhados sob a tabela-cartão (`testhist-chess-light-720.png` rodapé). | `white-space: nowrap` em `.ds-button` e `.button`; largura mínima 88px. |
| RD-07 | P1 | métricas | Cabeçalho de `MetricCard` tem dois rótulos ("SCENARIO PASS RATE  attention", "REPORTED COST  usd") em `flex space-between` sem wrap (`primitives.css:251-262`): a 390 em 2 colunas colidem/quebram ("OBJECTIVE  needs / SCENARIOS  attention", `exec-failed-light-390.png` topo; "TOTAL TOKENS latest" `overview-light-390.png`). | Em `compact`: tom vira ponto colorido antes do valor; label em linha própria. |
| RD-08 | P1 | header | Descrição do header truncada a 390 ("Executions · evidence, plans…", "Tests · evidence, plans and li…", `testhist-chess-light-390-part1.png`) e a 720 ("Tests · evidence, plans and evaluati…", `tests-light-720.png`). | S-03 (descrição curta ou vazia em narrow). |
| RD-09 | P2 | Plans | A 720 o stepper 01/02/03 quebra 2+1 e "new local plan" ocupa uma linha inteira (`plans-light-720.png`); a 390 há 6 blocos (eyebrow, h1, sumário, 3 passos, 4 KPIs) antes do primeiro plano em y≈1.130 (`plans-light-390.png`). | Em narrow: esconder o stepper, KPIs 2×2 compactos (`ds-metric-card` 12px), lista primeiro. |
| RD-10 | P2 | Plan new / diálogos | A 390 o painel "review setup" + CTA "Create draft plan" vem depois da lista de 45 testes (`plan-new-light-390.png`, CTA a ~3.000px); em `dlg-newtest-light-720.png` o "Generated Markdown" + "Create test" ficam abaixo dos critérios (CTA a ~2.000px). O diálogo a 720 ainda tem margens/raio de modal. | ≤720: diálogo = sheet 100% com barra de ações sticky no rodapé (CTA + Cancel). |
| RD-11 | P2 | raiz | `overflow-x: hidden` na raiz (`DashboardShell.tsx:249`, `console-overrides.css:18`) + `min-width: 320px`: em painéis do console < 320px o conteúdo é cortado sem scroll; e qualquer elemento que escape (header actions, S-02) some em vez de rolar. | Remover `overflow-x-hidden` da raiz; cada tabela/gráfico gere o seu `overflow-x:auto` com `scroll-shadow`. |
| RD-12 | — | referência positiva | Test history (`testhist-chess-light-720.png`, `…-390-part1.png`): filtros 5→2→1 colunas, KPIs 5→2→1, tabela vira cartão com rótulos — é o modelo a generalizar. | Extrair `.history-table` (`index.css:9459+`) para a `Table` do DS. |

### Regras gerais propostas
- **≤720 (container `narrow`)**: tabelas mantêm 3 colunas — identidade (nome + data), estado (StatusBadge), ação — e movem as restantes (SCOPE, OUTCOME, EFFICIENCY; VERSION, COMPLEXITY, HUMAN HORIZON, REALISM, CALIBRATION, EXECUTIONS) para uma linha de metadados mono 12px sob o nome. Nunca scroll horizontal em listas.
- **≤480 (`compact`)**: linha vira cartão (rótulo 11px + valor), ação no rodapé do cartão; grids de KPI 1 coluna; header do console só com título + 1 ação + "×".
- **KPIs**: 4 → 2 (≤720) → 1 (≤480); palavra de tom vira ponto.
- **Asides** (Quick run, review setup, Catalog): viram barra sticky no rodapé do container em narrow.
- **Diálogos**: sheet full-screen ≤720 com CTA sticky.
- **Medir pelo container**, não pelo viewport (DS-12).

### Quick wins
- `.ds-button`, `.button`: `white-space: nowrap`.
- `OverviewPage.tsx:455`/`TestsCatalogPage.tsx:537`: trocar `min-w-[56rem]/[82rem]` por colunas `hidden md:table-cell` nas secundárias (30 min).
- `primitives.css:251`: `flex-wrap: wrap` no `.ds-metric-heading`.

---

## Acessibilidade transversal (A11Y)

### Anatomia atual
- Foco: `:focus-visible` outline 2px accent, offset 3px (`dashboard-shell.css:157-161`) + `box-shadow` extra em `.ds-root` (`foundations.css:60-64`). `prefers-reduced-motion` respeitado (`dashboard-shell.css:163-168`, `foundations.css:95-114`).
- Landmarks: `<main>` do shell (`PageMain`, `DashboardShell.tsx:246`; em standalone é `<main>`, `standalone-console-ui.tsx:90-100`) **e** `<main>` por página (Overview :697, Plans :423, Coverage :19, Execution :631/660/704, LocalPlan :1651/2127, TestsCatalog :388). Skip links em 5 páginas (`.skip-link`, `index.css:252-269`).
- Tabelas semânticas (`<table>`) em Overview, Tests catalog, Test history, Compare, Plan detail, AssessmentWorkspace; Plans e "scenario results" do exec detail são cartões/botões.
- Headings: Overview/Executions/Tests catalog sem h1 (JSON `headings` começa em H2); Execution detail H1→H2→H4; Plans H1→H2→H3.
- Formulários: filtros do Overview/Executions com `<label>` + `.visually-hidden` ("Search executions", "Filter by status/trigger", `OverviewPage.tsx:416-450`) — correto; selects do shell com `aria-label` (`DashboardShell.tsx:261, 292`).

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| A11Y-01 | P0 | contraste (texto) | `--text-muted #a3a09c` sobre `#fafafa` = **2.49:1**; sobre fill `#ededed` = **2.22:1**. Usado em `.ds-page-context`/`.ds-metric-heading` (`primitives.css:203, 256`, 11px uppercase), cabeçalhos de coluna (`OverviewPage.tsx:455` `text-[var(--color-ink-ghost)]`), eyebrows, datas de linha (10.4px), "20 of 20 executions", ids de plano (9.3px). Falha AA (4.5) e AA-large (3.0) em todas as telas light. Dark `#6f6f6f` = 3.76:1 (DK-01). | `--color-ink-ghost` light ≥ `#767370` (4.51:1 em #fafafa; 4.02 em fill → usar 12px) e dark ≥ `#8a8a8a`; `#a3a09c` só para separadores/ícones. |
| A11Y-02 | P0 | contraste (estado) | `--danger #ff0026` sobre `#fafafa` = **3.81:1** (fill 3.40): todo "failed", "hard gate failed", "judge error", "50%", "Needs attention", "1/2" a 12-13px falha AA em `overview-light`, `executions-light`, `exec-failed-light`, `testhist-chess-light`, `compare-light`, `plan-sysprompt-light`. `--warning #a87a00` = **3.69:1** (fill 3.29): "retired", "inconclusive", "attention", "ai concerns". Só `--success` (5.76) e `--accent` (5.26) passam. | Pedir ao host variantes de texto: alert `#c4001d` (5.98/5.33), warn `#8a6400` (5.15/4.60); ou seguir o próprio vocabulário: **ponto colorido + texto em `--text`** (`StatusBadge` já tem o ponto; basta `color: var(--text)` no texto). |
| A11Y-03 | P0 | tamanho | 17 tamanhos < 11px renderizados; 165 declarações `< 0.6875rem` em `index.css` (ex.: `.chart-contract-label` 9px `:920`, `.matrix-key-score` 0.55rem `:1801`, `.efficiency-table thead th` 0.58rem `:1426`, `.config-card dt` `:2647`, `.diagnostic-table th` 0.56rem `:2960`, `.detail-metric small` 0.57rem `:2781`) + `text-[0.58rem]` ×10, `text-[9px]` (LOCAL). Plan new/detail: 13 tamanhos < 11px cada (8px, 8.96, 9.12…). | Piso 11px para tudo o que se lê; 12px para dados; remover `small` aninhado em `small`. |
| A11Y-04 | P1 | alvos | Header actions 28px (`AppHeader.tsx:23` `min-h-7`), `ds-button-compact` 28px (`primitives.css:63`), "Set A" ≈24px e "View details" ≈26px (`testhist-chess-light-1440.png`), "View metrics" ≈26px ×47, tabs ≈36px, `×` do console 36px. Nenhum alvo atinge 44px; vários < 32px; em narrow (RD-06) ficam ainda mais próximos. | Mínimo 32px + 8px de gap; 40px em `narrow`; remover `compact` de ações de linha. |
| A11Y-05 | P1 | landmarks | `<main>` aninhado: shell + página (locais na anatomia). Só pode existir um `main` e não pode estar dentro de outro. `<nav>` só em breadcrumbs (`ExecutionPage.tsx:708`, `LocalPlanPage.tsx:1671`); as seções não são `nav` (S-05). | Páginas → `<section aria-labelledby="page-title">`; `main` único no shell; seções em `<nav>`. |
| A11Y-06 | P1 | headings | Overview, Executions, Tests catalog: **sem h1** (primeiro heading H2 "e2e::* control-plane run"/"Recent executions"); Execution detail salta H2→H4 ("Local Calc Smoke"); o título do console ("harness e2e") não é heading (standalone `<strong>`, `standalone-console-ui.tsx:52`). Os h2 dos diálogos fechados ("Name this run", "Configure the execution environment"…) aparecem no DOM de todas as páginas Overview/Executions. | h1 = nome da seção/entidade em cada página (`PageHeader headingLevel=1`); h2 blocos; h3 subblocos; `<dialog>` montado só quando aberto. |
| A11Y-07 | P1 | skip links | `ExecutionPage.tsx:628-630`: o "skip link" é "Back to executions" e navega para fora; `:700-702` aponta para `hashForExecution(...)` (muda a rota em vez de focar `#main`). Tests catalog, Test history e Compare não têm. `.skip-link` é `position: fixed; top:-72px` (`index.css:252-269`) — dentro do console aparece no canto do **viewport**, fora do painel. | Um skip link no shell → `#harness-e2e-content` (com `tabindex=-1`), posicionado no topo do `PageMain`. |
| A11Y-08 | P1 | nomes acessíveis | Botões de expandir cenário no exec detail têm como nome todo o conteúdo concatenado: `exec-failed-light-390.json` → "Local Calc Smoke v1openai-codex-codex-gpt-5-6-terra · 1 runObjective resultJudge ErrorAdvisoryAI inconclusiveRuntime1m 40savgStructureStandard". "View metrics" ×47 e "open plan →" ×7 são ambíguos fora de contexto (`tests-light-1440.json`, `plans-*.json`). | `aria-expanded` + `aria-label="Expand Local Calc Smoke"`; ações de linha com `aria-label="View metrics for chess_play_ladder"` ou nome como link. |
| A11Y-09 | P1 | não-texto | Limites de inputs/selects 1.12:1 (light) / 1.13:1 (dark) — DK-03; linha de tabela e cartões de fill 1.12:1 (aceitável para cartões, não para controles). Ícones de tab `stroke 1.35` em `--text-soft`. | Bordas de controle ≥ 3:1 (`rgba(20,16,8,.5)` light = 3.53:1). |
| A11Y-10 | P1 | estado atual | `aria-current` só em código morto (`SectionNav.tsx:43`) e `LocalPlanPage.tsx:1687`; tabs usam `aria-selected` (ok) mas Coverage não tem tab selecionada (S-06); breadcrumb do exec detail sem `aria-current="page"` no último item (`ExecutionPage.tsx:716`). | `aria-current="page"` nas seções (como links) e no último crumb. |
| A11Y-11 | P2 | uppercase 11px | Uppercase + tracking em 11px mono com cor muted é a pior combinação do app para baixa visão/dislexia (todas as th, eyebrows, rótulos de métrica). | 11px só com `--text-soft`; considerar lowercase mono (já é o vocabulário) e reservar uppercase a th. |
| A11Y-12 | P2 | foco | Dois estilos de foco (outline em `dashboard-shell.css:158-160`, outline + box-shadow em `foundations.css:60-64`); fallback `--color-rule-focus` → accent (4.69:1 sobre fill, ok). | Um só estilo no shell; remover o de `.ds-root`. |
| A11Y-13 | P2 | live regions | Banner de alerta com `aria-live="polite"` (`OverviewPage.tsx:352`) ok; "20 of 20 executions", estados "running/cancelling" e "Waiting for data" (Coverage) sem `aria-live`; `LegacyLoadError` tem `role="alert"` ok. | `aria-live="polite"` no contador de resultados e no status de execução em curso. |

### Oportunidades de redesenho
- **Tokens de texto acessíveis por padrão**: `--text-muted` deixa de ser cor de texto; status = ponto + texto em `--text`; a tabela de contraste passa a fazer parte do DS (`foundations.css`).
- **Estrutura de página única** (h1 → h2 → h3, `main` único, `nav` de seções, skip link do shell) aplicada via `PageHeader`/`PageShell` — as páginas deixam de montar cromo.

### Quick wins
- `dashboard-shell.css:38`: `--text-muted: var(--color-ink-ghost, #767370)`; dark idem com `#8a8a8a`.
- `primitives.css:123-140`: `.ds-status-badge { color: var(--text) }` mantendo `--ds-status-color` só no ponto.
- Remover `<main>` das páginas (7 ocorrências) → `<section>`.
- `ExecutionPage.tsx:628-630`: renomear a classe do link "Back to executions" (não é skip link).

---

## Coverage (rota `#/ext/harness-e2e/coverage`, sem item de menu)

### Anatomia atual
- `pages/CoveragePage.tsx`: `LegacyLoadError` → `skip-link` → `.ambient` ×2 (`display:none`, `index.css:271`) → `DashboardPageActions active="coverage"` sem ações (:17) → `<main class="page-shell overview-shell">` → `.page-heading` com eyebrow `.live-dot` "Harness stack", `<h1>` "Code coverage", parágrafo → `.sync-block` "Last published / Waiting for data" (`<time dateTime="">`) → `#empty-state` (glifo "⌁" 40px, h2, p) → `#coverage-content` (2 `article.panel.coverage-card`: h2, `.coverage-percent`, `.coverage-meta`, link `.button.button-secondary` "Browse report" para `./coverage/{integration,e2e}/index.html`).
- Dados: `hooks/useLegacyPage.ts` manipula o DOM imperativamente (`querySelector('#empty-state').hidden`, :38-74) a partir de `window.HARNESS_COVERAGE`, carregado por `./coverage/summary.js` **apenas em standalone** (`bootCoverage` :76-80 via `loadLegacyPage`); embutido chama só `renderCoverage()` (:95-98).
- Captura: `coverage-light-1440.png` — header sem ações, nenhuma tab selecionada, eyebrow laranja, h1 "code coverage", "LAST PUBLISHED / Waiting for data" e, ao mesmo tempo, o empty state "no coverage published yet". 10 tamanhos de fonte (10.08 → 40px) para 7 textos.

### Achados
| # | Sev | Categoria | Achado (evidência) | Recomendação concreta |
|---|-----|-----------|--------------------|------------------------|
| CV-01 | P0 | dados | No console a página **nunca** mostra dados: em modo embutido `useLegacyPage` chama `renderCoverage()` sem carregar `./coverage/summary.js` (`useLegacyPage.ts:94-98` vs `:76-80`) → `window.HARNESS_COVERAGE` é `undefined` → empty state permanente (`coverage-light-1440.png`). | Servir o sumário pela mesma bridge de dados das outras páginas (`DashboardDataBridge`) e renderizar em React; ou remover a rota. |
| CV-02 | P1 | links | `href="./coverage/integration/index.html"` (`CoveragePage.tsx:63, 82`) é relativo ao documento do host (`127.0.0.1:3113/`) → 404 quando embutido. | URL absoluta vinda da config/bridge; abrir em nova aba com `rel="noopener"`. |
| CV-03 | P1 | IA / órfã | Nenhum link de entrada (grep `hashForCoverage`/`/coverage` fora do router e do shell: 0), fora do menu (`DashboardShell.tsx:146-152`), sem ação no header; só por URL digitada. O card "Catalog · 47 tests · revision 79bb…" do Overview seria o lugar natural. | Decidir: (a) card "Coverage" no Overview + entrada em Tests, ou (b) apagar rota, página, `.coverage*` (7 regras) e `useLegacyPage`. |
| CV-04 | P1 | navegação | Nenhuma tab ativa e, em narrow, o `<Select>` mostraria "Overview" (S-06). | Ver S-06. |
| CV-05 | P1 | estados | "Last published: Waiting for data" e "No coverage published yet" aparecem juntos (`coverage-light-1440.png` y≈260 e y≈416) — contraditório; não há estado de carregamento nem de erro (`loadScript(..., false)` engole falhas, `useLegacyPage.ts:78`); `<time dateTime="">` vazio. | Um único estado: loading → dados / vazio / erro; `sync-block` só com dado. |
| CV-06 | P1 | DS | 100% legado: eyebrow com `.live-dot` laranja (sugere "ao vivo" para um relatório diário), h1 lowercase mono mas "Last published"/"Waiting for data" em sans bold, `.empty-state` com glifo "⌁" 40px, `.panel.coverage-card` com borda, `.button-secondary` legado. | `PageHeader` (context "harness stack", h1, summary, actions = "browse report") + 2 `MetricCard` ("integration 71.2%", "e2e 54.0%", detail "12.3k of 17.2k lines · Aug 26") + estado vazio do DS. |
| CV-07 | P2 | copy | "refreshed by the daily E2E cycle" / "The next scheduled Harness E2E Daily workflow will publish the first reports here" promete um fluxo que, embutido, nunca escreve aqui (CV-01). | Copy honesta ("published by CI · see workflow") com link para o artefato. |
| CV-08 | P2 | consistência | `document.title` "Harness stack coverage" (`App.tsx:65`) ≠ h1 "Code coverage" ≠ descrição do header "Coverage"; ícone da seção duplica o de Executions (`DashboardShell.tsx:138-143`). | Um nome: "coverage". |

### Oportunidades de redesenho
- **Coverage como card do Overview** (2 métricas + link "browse report") e como coluna opcional em Tests — deixa de ser página.
- Se ficar página: `PageHeader` + grelha de `MetricCard` por suíte + histórico (sparkline) — 40 linhas de React, zero CSS legado.

### Quick wins
- Esconder `.sync-block` enquanto não há dado (`hidden` até `updated_at`).
- Trocar `.live-dot` por nada (é um relatório, não um estado ao vivo).
- Marcar a rota como `noindex`/remover do `document.title` até CV-01 ser resolvido.
