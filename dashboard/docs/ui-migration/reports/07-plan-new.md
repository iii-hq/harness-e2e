## Criação de plano local (rota `#/ext/harness-e2e/plans/new`)

### Anatomia atual (5-10 linhas)
- `DashboardPageActions active="plans"` (AppHeader.tsx:38-61) só registra a seção no cabeçalho do console; nenhuma ação de página é publicada — o console mostra apenas "Plans · evidence, plans and live evaluation control" e o "×". Nas larguras 720/390 a barra Overview/Tests/Executions/Plans some por completo (DashboardShell.tsx:287 `harness-e2e-navigation-narrow hidden`; probe: navWideVisible=false, navNarrowVisible=false).
- `<main id="plan-create-main" class="ds-root …">` (LocalPlanPage.tsx:1626-1630) → `<header>` com eyebrow mono "EXECUTION SETUP", h1 "create a benchmark plan" (mono lowercase pela regra global dashboard-shell.css:121-126), lede, e um `<nav aria-label="Execution setup mode">` de 2 caixas com borda "Quick execution | Reusable plan" (1640-1667) — o primeiro é um link para o Overview + `sessionStorage` (ExecutionSetup.tsx:10-12).
- Banner de erro `role="alert"` "Plan cannot be created" (1685-1697), renderizado ACIMA do formulário, só quando o bridge falha ou o create rejeita.
- `<form class="… rounded-xl border … lg:grid-cols-12">` (1706): coluna 8/12 = `ExecutionSetup mode="plan"` (ExecutionSetup.tsx:117-508): barra de status (ponto + "45 registered models · 45 scenarios" + URL em `<code>`), seção **01** "define the evidence intent" (Plan label obrigatório + Purpose textarea), **02** "configure the execution environment" (iii WebSocket URL editável e `required`, `ProviderModelDropdown` Execution model obrigatório, Judge model com placeholder "Use the default judge"), **03** "select the benchmark scope" (busca `type=search`, "Select visible (45)", "Clear", `<output>` "0 tests selected / 0 logical runs", grid 2 colunas de cards-checkbox com `max-h-[25rem]` e scroll interno), e um `<details>` "OPTIONAL CONTROLS / Sampling, retries and seed" (439-505) com Runs per test, Technical retries, Case seed.
- Coluna 4/12 = `ExecutionSetupReview` (510-607): `<aside lg:sticky>` com eyebrow "REUSABLE WORKFLOW", h2 "review setup", pílula "Incomplete/Ready", 4 tiles `<dl>` (Tests, Logical runs, Runs/test, Retries) em 20px, lista Execution model / Judge / Harness endpoint, parágrafo `#plan-create-requirements` ("Before creating: add a plan label."), botão `type=submit` "Create draft plan" (desabilitado até `canCreate`, 1565-1570/1793) e link "Cancel" → `#/plans`.
- `<footer>` legado "Harness E2E · local plans / Back to home" (1810-1823) → Overview.
- `DiscardDraftDialog` (131-183): `<dialog open aria-modal="true">` sem `showModal()`, disparado por `useDirtyNavigation` (hooks/use-dirty-navigation.ts) em `hashchange`/`beforeunload`.
- Página real: 1653px de altura a 1440 (a captura de 2768px inclui o scroll interno da lista); botão criar em y=987 (1440), y=2212 (720), y=2571 (390).

### Achados
| # | Sev | Categoria | Achado (o que está errado, com evidência: screenshot + arquivo:linha) | Recomendação concreta |
|---|-----|-----------|-----------|----------------|
| PN-01 | P0 | feedback/bug | O formulário nasce "sujo": `initialValues.current.technicalRetries = '1'` (LocalPlanPage.tsx:1511) enquanto o estado inicial é `'0'` (1497) → `dirty` (1573-1583) é `true` desde o mount. Clicar em "Cancel", em qualquer aba da nav, em "Quick execution" ou "Back to home" sem ter digitado nada abre "Discard draft changes?" (plan-new-cancel-untouched-light-1440.png, `dialogOpen=true`). O usuário aprende a ignorar o diálogo. | Corrigir para `technicalRetries: '0'` e derivar `initialValues` do mesmo objeto usado no `useState` (uma constante `EMPTY_PLAN_FORM`), com teste em LocalPlanPage.test.tsx cobrindo "formulário intocado não pede confirmação". |
| PN-02 | P0 | responsivo | Abaixo de `lg` (1024px) o `aside` de revisão deixa de ser sticky e vai para o fim do formulário (ExecutionSetup.tsx:531 `lg:sticky`; LocalPlanPage.tsx:1706 `lg:grid-cols-12`). A ação primária e a única mensagem de validação ficam a 2212px (720, viewport 900) e 2571px (390, viewport 800) — 2-3 telas abaixo dos campos (plan-new-light-720-part3.png, plan-new-light-390-part3.png). Quem preenche o label não vê o que falta nem onde criar. | Barra de ação sticky no rodapé da viewport em todas as larguras: "45 tests · 45 runs — [Cancel] [Create draft plan]" + mensagem de pendência; os tiles de revisão viram uma linha de resumo compacta acima do botão. |
| PN-03 | P0 | a11y/feedback | `DiscardDraftDialog` renderiza `<dialog open aria-modal="true">` sem `showModal()` (LocalPlanPage.tsx:150-156): probe confirma `:modal=false`, sem backdrop, Escape e clique fora não fecham, a página atrás continua clicável e o foco não é preso. Além disso o container rola para o topo quando o guard dispara (scrollTop 566→0 a 1440; 2062→0 a 390) — "Keep editing" devolve o usuário ao topo, longe do campo (c-dialog-light.png, c-dialog-390.png). | Abrir via `ref.current.showModal()` em `useEffect` e fechar em `onCancel`/Escape; preservar scroll (não chamar `replaceState` até o usuário decidir, ou restaurar `scrollTop` após o `rerender`). Melhor ainda: usar a primitiva de modal do DS compartilhada pelo dashboard. |
| PN-04 | P0 | a11y/contraste | Texto `--color-ink-ghost` #a3a09c sobre #fafafa = 2,49:1 (sobre #f7f5f2 = 2,39:1; dark #6f6f6f/#111 = 3,76:1) carrega informação essencial: descrições de seção 12px (ExecutionSetup.tsx:107), "Before creating: add a plan label." 12px (LocalPlanPage.tsx:1765), contagem "0 tests selected / 0 logical runs" 10,88px (:377), ids dos testes 9,92px (:423), rótulos `<dt>` 9,92px (:565), pílula "Incomplete" 9,92px (:548), "required/optional/automatic when blank" (:226, :291, :308), sublabels "One result now / Baseline + candidates" 10,4px (LocalPlanPage.tsx:1650, 1662). Medido no probe: `req.color rgb(163,160,156)`, `code.fs 9.92px`. | Reservar ghost para texto decorativo; tudo que orienta (descrições, pendência, contagem, ids) em `--color-ink-faint` (#6b6865 = 5,3:1) e ≥ 11px. A mensagem de pendência em ink com ícone. |
| PN-05 | P0 | validação | Não existe validação inline: o submit fica `disabled` até `canCreate` (1565-1570, 1793), então os `required` de label/URL (ExecutionSetup.tsx:234, 282) nunca disparam; Enter no campo label não faz nada (probe: sem `[role=alert]`, sem `:invalid`, hash inalterado). A única orientação é o parágrafo ghost no aside (PN-04), sem `aria-invalid`, sem foco no erro; a mensagem do `create()` (1590-1594) só existe se o botão pudesse ser clicado. O banner de erro (1685-1697) fica acima do formulário: um erro do servidor após rolar até a lista de testes aparece fora da tela. | Habilitar o botão, validar no submit: mensagem sob o campo (`aria-describedby`, `aria-invalid`, cor alert, 12px ink), `focus()` no primeiro campo inválido, resumo "2 pendências" ao lado do botão. Erro de servidor renderizado junto ao botão (no aside/barra de ação) e anunciado por `aria-live`. |
| PN-06 | P1 | IA/escopo | Testes locais (Markdown) são excluídos silenciosamente do escopo do plano: `catalogValue` filtra `local_scenarios` (LocalPlanPage.tsx:90-108) → a barra diz "45 scenarios" enquanto o catálogo Tests lista 47 e o Quick execution aceita locais com badge "Local" (LocalRunnerDialog.tsx:370-372; ExecutionSetup.tsx:417-421). Nenhuma nota explica a diferença. | Se planos não suportam locais, mostrar "2 local tests are not available in plans" na seção 03 (ou listá-los desabilitados com o badge); se suportam, passar `localScenarioIds`/`scenarioTitles` como no runner. |
| PN-07 | P1 | modelo/a11y | `ProviderModelDropdown`: grupos de provider começam colapsados (ProviderModelDropdown.tsx:50-52) → escolher um modelo exige 3 cliques (abrir → expandir "anthropic 9" → escolher; c-model-expanded-light.png). Teclado: ArrowDown abre mas o foco fica no gatilho (probe `active: "Execution model"`), Tab cai no botão do provider, Escape dentro do menu não fecha (`stillOpen:true`; onKeyDown só no gatilho, :106-120). ARIA: `role=listbox` contém `<section>` e botões sem `role=option` (:164-186); `required` só como `data-required` (:103); o rótulo visível "Execution model" é `<div>/<span>` sem `for` (ExecutionSetup.tsx:288-304). | Expandir o único provider por padrão (ou todos quando ≤3), roving tabindex + setas + typeahead, Escape/clique fora fecham de qualquer ponto, `aria-required`, `aria-labelledby` para o rótulo visível, `role=group` para providers. Alternativa barata: `<select>` nativo com `<optgroup>`, estilizado pelo DS. |
| PN-08 | P1 | juiz | Depois de escolher um juiz não há como voltar ao padrão: o listbox só tem modelos (ProviderModelDropdown.tsx:189-210) e não existe botão de limpar; a dica "automatic when blank" (ExecutionSetup.tsx:308-310) descreve um estado irrecuperável sem recarregar a página (plan-new-judge-open-light-1440.png). | Primeira opção "Default judge (automatic)" no menu do juiz e/ou "×" no gatilho quando há valor. |
| PN-09 | P1 | densidade/escopo | Lista de 45 testes em grid 2×N de cards de 64px dentro de `max-h-[25rem]` (ExecutionSetup.tsx:389): 400px visíveis para 1664px de conteúdo (probe `list.scrollH 1664`) → scroll dentro de scroll, ~5 linhas visíveis, sem agrupamento, sem ordenação, sem filtro "selected only"; o feedback de seleção é só a contagem. Com busca ativa, "Select visible (2)" e "Clear" ficam lado a lado mas "Clear" apaga os 45 (366-373), não só os visíveis (c-search-light.png). A 390 vira 1 coluna com 5 cards visíveis de 45 (plan-new-light-390-part2.png). | Lista compacta (linha de 36px: checkbox, nome, id em 11px) agrupada por família (chess, engineering, cross_*, …) com "select group"; chip "Selected (n)" que filtra; sem `max-h` (rolagem da página) ou virtualização com altura 60vh; "Clear" → "Clear selection (45)" e "Deselect visible" quando há filtro. |
| PN-10 | P1 | consistência DS | Nenhuma primitiva do DS é usada: botões sans 12px/14px semibold mixed-case com 40/44px (ExecutionSetup.tsx:77-78; LocalPlanPage.tsx:1793; probe `selectVisible.h 40`, `submit.h 44`) vs mono 13px lowercase 36px; labels sans 12px semibold (`fieldLabel` :73-74) vs mono 0,6875rem uppercase; container `rounded-xl` = 12px (1706; probe `radii.form 12px`) vs raio único 6px; tudo por borda + grade hairline `gap-px bg-[var(--color-rule)]` + cards e inputs com borda 1px, contra "hierarquia por preenchimento"; numeração "01/02/03" em accent (ExecutionSetup.tsx:100) é o padrão legado citado no brief. Títulos de seção viram mono lowercase pela regra global, mas "Sampling, retries and seed" é `<strong>` sans mixed-case (:445) — dois estilos de título lado a lado (plan-new-light-1440-part2.png). | Migrar para `primitives.tsx` (Button, Field/Label, Panel por preenchimento), `rounded-[6px]`, remover bordas de cards/inputs em favor de `--surface-fill`; título do `<details>` como h2/h3 para herdar o vocabulário. |
| PN-11 | P1 | tipografia | Cinco tamanhos abaixo de 11px na mesma tela (JSON `sizes`: 9,6 / 9,92 / 10,24 / 10,4 / 10,88px): 0,58rem badge "Local" (:418), 0,62rem ids/dt/pílula (:423, :565, :548), 0,64rem provider no gatilho (ProviderModelDropdown.tsx:128), 0,65rem eyebrows (:442, :533) e sublabels do nav (LocalPlanPage.tsx:1650), 0,68rem números de seção, URL e contagem (:100, :193, :377). O DS define um único label (0,6875rem). | Colapsar em dois tamanhos: 11px (labels/eyebrows/ids mono) e 12px/13px (corpo). |
| PN-12 | P1 | layout/controles | Quatro alturas de controle nas seções 01-02: input "Plan label" estica para 60px porque divide a linha do grid com a textarea de 76px (ExecutionSetup.tsx:222-263; probe `input.h 60`), URL 44px, dropdowns 51px por terem duas linhas (ProviderModelDropdown.tsx:98, 122-131), botões 40px, Create 44px (plan-new-light-1440-part1.png: label visivelmente mais alto que o dropdown). | `self-start` no campo label (ou `items-start` no grid), gatilho do dropdown em uma linha "anthropic / claude-fable-5", altura única 36px para inputs/botões/dropdowns (compact 28px). |
| PN-13 | P1 | affordance | O `<details>` "Optional controls" esconde o marker e não tem chevron (ExecutionSetup.tsx:440 `marker:content-none … [&::-webkit-details-marker]:hidden`; probe `summaryChevron=false`); a linha de 92px com "1 / test · 0 retries" à direita parece um rodapé estático, não um painel expansível (plan-new-light-1440-part2.png vs c-advanced-light.png). | Adicionar `ChevronDown` com `group-open:rotate-180`, ou promover a seção "04 sampling" sempre visível em 3 campos compactos. |
| PN-14 | P1 | dark | No escuro os campos dependem de preenchimento 1,05:1 (#171717 sobre #111111) + borda 7% branco (probe `input.border rgba(255,255,255,0.07)`): limites de inputs, cards e a grade hairline quase desaparecem; a pílula "Incomplete" e os `<dt>` em #6f6f6f ficam a 3,57:1 sobre o aside (plan-new-dark-1440-part1.png, part2.png). | Usar `--surface-fill` rgba(255,255,255,.055) sem borda e hover .085 (tokens do DS), placeholder em ink-faint, pílula com texto ink-faint. |
| PN-15 | P1 | hierarquia/ações | A ação primária desabilitada em `opacity-45` (LocalPlanPage.tsx:1793) é o maior bloco colorido da tela: fundo efetivo #dba48c com texto #f8f7f6 = 2,02:1 (dark 2,41:1) — um "botão salmão" inerte domina o aside (plan-new-light-1440-part2.png). No cabeçalho, o `<nav>` "Quick execution | Reusable plan" (1640-1667) parece um toggle de modo, mas "Quick execution" navega para o Overview e abre o runner via `sessionStorage` — e, com PN-01, dispara o diálogo de descarte. O `<span aria-current="page">` não é link, então o nav anuncia um único link. | Botão sempre habilitado (ver PN-05) ou estilo disabled quiet (preenchimento .055 + texto muted). Mover a escolha quick/plan para o ponto de entrada (Plans → "New plan ▾ quick run / reusable plan") ou tornar um tab real que troca `mode` do mesmo `ExecutionSetup` sem sair da página. |
| PN-16 | P1 | copy | Jargão sem explicação: "Define the evidence intent", "Logical runs", "Logical evidence samples", "Case seed — Canonical when blank", "Technical retries — Recovery attempts only", "iii WebSocket URL", "Baseline + candidates" (ExecutionSetup.tsx:215, 385, 461, 489-491, 475, 277; LocalPlanPage.tsx:1662). "test" e "scenario" alternam na mesma tela: "45 scenarios" (LocalPlanPage.tsx:1732) vs "tests selected" (:381), "Find a test" (:339), "Runs per test" (:459). | Padronizar em "test"; títulos diretos ("Name the plan", "Choose model and judge", "Pick the tests"); descrições que explicam consequência ("Runs per test: each test runs N times; more runs = comparison more reliable"; "Retries only recover crashes, they don't add samples"). |
| PN-17 | P1 | estados | Catálogo carregando/indisponível: a seção 03 mostra "No tests match “”. Try another name or clear the search." porque `visibleScenarios` está vazio com query vazia (ExecutionSetup.tsx:430-433); dropdowns viram "Choose a model" desabilitados sem explicação (:300, :316); a barra diz "Catalog unavailable" / "URL not loaded" em 10,88px ghost (:193; LocalPlanPage.tsx:1730-1734); o `onRefreshCatalog` existe no componente (:196-210) mas a página de plano não o passa (1716-1748), então não há "retry" quando o Harness está fora. Só o banner de erro no topo (1685) quando o bridge falha. | Estado explícito por seção: skeleton durante `loading`; "Harness offline at ws://… — [Retry]" na seção 02/03 com `onRefreshCatalog`; texto de vazio distinto para "catálogo vazio" vs "sem resultado da busca". |
| PN-18 | P1 | IA/navegação | Sem caminho de volta no topo: nenhum breadcrumb "Plans › New plan" (o console mostra só "Plans ·…"); as saídas são o "Cancel" no fim do aside (y=1043 a 1440; y=2627 a 390) e o rodapé legado "Back to home" que vai para o Overview, não para Plans (LocalPlanPage.tsx:1810-1823). Em ≤720 as abas de seção somem (DashboardShell.tsx:287 `hidden`; plan-new-light-720-part1.png) — no celular a única saída é o Cancel no fundo. | Link "← Plans" no header da página (ou ação "cancel" publicada via `DashboardPageActions actions`), remover o footer; corrigir a nav estreita no shell (cross-ref S). |
| PN-19 | P1 | a11y/semântica | Dois landmarks `<main>` (console + `<main id="plan-create-main">`, LocalPlanPage.tsx:1626; probe `mains=2`); rótulos do dropdown não associados (PN-07); `<output>` da contagem sem `aria-live` explícito enquanto a barra de status inteira tem `aria-live="polite"` (ExecutionSetup.tsx:184) e anuncia URL a cada mudança. Alvos: checkbox 16px dentro de card-label 64px (ok), botões 40/44px (ok), links do footer mono 12px (abaixo de 24px de altura). | `<section aria-labelledby="plan-create-title">` em vez de `<main>`; `aria-live` na `<output>` de seleção (não na barra); remover footer. |
| PN-20 | P2 | redundância | A URL do Harness aparece 3× (barra de status :193, campo editável :278, aside "Harness endpoint" :593-598) e o modelo 2× (gatilho + aside). Os 4 tiles de revisão dão numerais de 20px a valores triviais ("Runs / test 1", "Retries 0") com o mesmo peso de "Tests 0" (plan-new-light-1440-part1.png). | Uma linha de resumo: "45 tests × 1 run · anthropic / claude-fable-5 · default judge"; URL só no avançado. |
| PN-21 | P2 | formulário | "iii WebSocket URL" editável e `required` no meio do fluxo principal (ExecutionSetup.tsx:273-287); editar não refaz o catálogo (nenhum efeito em `onUrlChange`, LocalPlanPage.tsx:1739) → lista de modelos/testes pode não corresponder ao endpoint digitado. | Read-only com "change" no avançado; ao mudar, refetch do catálogo e aviso. |
| PN-22 | P2 | validação numérica | Runs/retries/seed não aplicam `max` (só `Math.max(1, …)` em LocalPlanPage.tsx:1556-1557; `max="20"`/`max="3"` nos inputs :467, :482 não bloqueiam submit por o botão ser controlado) — 999 runs passam sem aviso; o resumo "1 / test · 0 retries" fica em mono ghost 12px. | Clamp + mensagem inline ("max 20"), stepper compacto. |
| PN-23 | P2 | diálogo/ações | "Discard and continue" usa `bg-brand` (mesma cor do "Create draft plan") para uma ação destrutiva; botões sans 14px 44px com borda (LocalPlanPage.tsx:159-181; c-dialog-light.png). Props invertidas (`onDiscard` liga em `cancelNavigation`, `onContinue` em `confirmNavigation`, 1612-1615) — funciona, mas confunde manutenção. | "Keep editing" primário/secundário neutro, "Discard" em estilo quiet com texto alert; botões do DS; renomear props para `onKeepEditing`/`onDiscard`. |
| PN-24 | P2 | busca | O `type="search"` deixa o "×" nativo azul do Chrome dentro do campo (c-search-light.png, c-nomatch-light.png) — única cor fora da paleta na tela. A busca não indica quantos dos selecionados estão ocultos. | `[&::-webkit-search-cancel-button]:appearance-none` + botão de limpar próprio; contagem "2 of 45 shown · 45 selected (43 hidden)". |
| PN-25 | P2 | headings/escala | h1 20px (`text-xl`, 1636) vs DS h1 1rem; "review setup" h2 16px (ExecutionSetup.tsx:538) vs seções h2 14px (:104); eyebrow "EXECUTION SETUP" duplica o rótulo de seção do console. | Usar `.ds-page-header` (primitives.css:211-220); um h2 de 1rem. |
| PN-26 | P2 | status | Ponto verde + "45 registered models · 45 scenarios" em sans 12px (ExecutionSetup.tsx:186-192) não segue "ponto 6px + mono lowercase" do DS; a cor do ponto só reflete `availableScenarios.length` (verde/warn), nunca "carregando". | Status mono lowercase ("catalog ready · 45 models · 45 tests"), ponto cinza durante loading, alert quando indisponível. |

### Oportunidades de redesenho (3-6 bullets)
- **Coluna única + barra de ação sticky**: trocar o split 8/4 por um formulário de ~720px e uma barra fixa no rodapé da viewport (resumo "45 tests × 1 run · claude-fable-5", pendências, Cancel, Create). Resolve PN-02/05/15/18/20 de uma vez e funciona igual a 1440/720/390.
- **Seletor de escopo como lista agrupada e filtrável**: linhas compactas de 36px agrupadas por família (chess, engineering, cross_*, browser…), "select group", chip "Selected (n)" que filtra, ids em 11px, sem scroll aninhado (ou virtualização a 60vh) e bandeja de selecionados no resumo. Resolve PN-09/06/24.
- **Combobox único para modelo/juiz**: campo com busca por "provider / model", "Default judge (automatic)" como primeira opção do juiz, navegação por setas e typeahead — ou `<select>` nativo com `<optgroup>` estilizado pelo DS (PN-07/08/12).
- **Fluxo único "New run" com modo dentro da página**: o mesmo `ExecutionSetup` já serve quick e plan; o toggle Quick execution / Reusable plan deveria trocar `mode` mantendo o estado do formulário (label, modelo, testes), em vez de linkar para o Overview via `sessionStorage`. A escolha do modo pode nascer no botão "New plan ▾" da lista.
- **Confirmação de descarte pela primitiva do DS**: um `ConfirmDialog` compartilhado (showModal, backdrop, foco preso, Escape, botões mono) usado aqui, no editor de cenário local e em outras ações destrutivas do dashboard; considerar autosave de rascunho (`localStorage`) para eliminar o diálogo.
- **Progressive disclosure de verdade**: seção "Advanced" do DS com WebSocket URL (read-only + change), runs/retries/seed e explicações em uma frase cada; o resumo "1 run per test" vira chip na barra de ação.

### Quick wins (só CSS/copy, < 1h cada)
- `technicalRetries: '1'` → `'0'` em LocalPlanPage.tsx:1511 (elimina o diálogo fantasma; PN-01).
- Trocar `text-[var(--color-ink-ghost)]` por `text-[var(--color-ink-faint)]` em descrições de seção, `#plan-create-requirements`, `<output>` de contagem, ids `<code>`, `<dt>` e pílula (ExecutionSetup.tsx:107, 377, 423, 565, 548; LocalPlanPage.tsx:1765).
- `showModal()` via `ref` + `onCancel` no `DiscardDraftDialog` (LocalPlanPage.tsx:143-156) e restaurar `scrollTop` após o guard.
- `self-start` no `<label>` do Plan label (ExecutionSetup.tsx:223) para parar o input de 60px; gatilho do dropdown em uma linha.
- `rounded-xl` → `rounded-[6px]` no `<form>` (LocalPlanPage.tsx:1706); remover `border` dos cards de teste e usar `bg-[var(--surface-fill)]`.
- Chevron no `<summary>` (`ChevronDown` + `group-open:rotate-180`, ExecutionSetup.tsx:440-456).
- Vazio correto: `availableScenarios.length === 0 ? 'Catalog not loaded' : 'No tests match "…"'` (ExecutionSetup.tsx:430-433); passar `onRefreshCatalog` na página de plano (LocalPlanPage.tsx:1716).
- Opção "Default judge (automatic)" no menu do juiz / `onChange('')`.
- Expandir o grupo do provider por padrão quando `groups.length === 1` (ProviderModelDropdown.tsx:50-52); `aria-required` e `aria-labelledby` no gatilho.
- Botões com as classes de botão do DS (mono 13px lowercase, 36px) em "Select visible", "Clear", "Cancel", "Create draft plan", "Keep editing", "Discard".
- "Clear" → "Clear selection (n)"; "45 scenarios" → "45 tests" na barra de status (LocalPlanPage.tsx:1732).
- Remover o `<footer>` "Harness E2E · local plans / Back to home" (1810-1823) e adicionar link "← Plans" no header da página.
- `[&::-webkit-search-cancel-button]:appearance-none` no campo de busca.
- `<main>` → `<section aria-labelledby="plan-create-title">` (LocalPlanPage.tsx:1626).
