{
  "profile": "code_patch",
  "baseline_must_fail": true,
  "public_commands": [
    {"program": "python3", "args": ["-m", "unittest", "discover", "-s", "tests", "-p", "test_*.py"], "timeout_seconds": 30}
  ],
  "hidden_commands": [
    {"program": "python3", "args": ["-c", "from src.config_cache import ConfigCache; f={'region':'file'}; e={'region':'env'}; c=ConfigCache(); assert c.resolve('api',f,e,{'region':'cli-a'})['region']=='cli-a'; assert c.resolve('api',f,e,{'region':'cli-b'})['region']=='cli-b'; assert f=={'region':'file'} and e=={'region':'env'}"], "timeout_seconds": 30}
  ]
}
