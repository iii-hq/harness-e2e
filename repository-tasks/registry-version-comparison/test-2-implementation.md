# Test 2 — Implement using the supplied reference plan

Work in `registry/`, which starts at the required commit. Read `inputs/requirements.md`, `inputs/reference-plan.md`, and `inputs/environment.json`. Implement the complete Registry version-comparison feature described there.

Use the supplied environment commands and URLs. Add focused automated tests, run the relevant project checks, start the real application, exercise the API and browser flows, and capture actual screenshots requested by the requirements. Do not generate or mock screenshots and do not publish anything.

Deliver the source changes in `registry/`. Write `output/report.md` in English with the starting SHA, changes made, exact commands and outcomes, deviations from the reference plan, limitations, runtime URLs exercised, and screenshot paths. Preserve raw logs and screenshots under `output/` when the environment supports them. Do not score the result.
